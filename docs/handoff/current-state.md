# VnTool 현재 상태 — 이어받는 세션을 위한 인수인계

기준: 2026-08-23 · 테스트 **1482 통과, 실패 0**
(Ked.Presentation.Core 343 · Vn.Core 60 · Vn.Authoring 757 · Vn.App 322)
> ⚠ 솔루션 단위 `dotnet test`는 **빌드에 실패한 프로젝트를 조용히 건너뛴다.**
> 요약 줄이 **넷**인지 세고 나서 "전부 통과"라고 말할 것.

> **⚠ 규격이 며칠 사이 v5 → … → v11(챕터) · v10(대본)로 굴렀다. 이 박스가 최신 계약이다.**
>
> **⚠ 셸 규칙 (2026-08-18 팀장 미팅)** — 탭은 **`챕터 그래프` → `연출 그래프` → `무대 프리뷰`** 순서다(왼쪽에서 오른쪽으로 밟으면 챕터가 완성된다 — 프리뷰 탭은 2026-08-20 승격, 우측 아래 도킹 패널과 분리 창을 대체). 옛 **"시나리오 그래프"가 "연출 그래프"로 개명**됐고 **기능 동결**이다: 기획자가 사실상 시나리오 작가여서 스토리는 챕터 그래프에서 끝나고, 저 판에 남는 일은 연출뿐이다. **휠만으로 확대·축소**(Ctrl 요구 해제, Ctrl+휠도 같은 배율). **⚠ 창 맨 왼쪽 열은 2026-08-22에 사라졌다** — 창은 **[탭들 | 스플리터 | 우측 열 460] 셋**이고, **챕터 목록은 챕터 그래프의 오른쪽 기둥 맨 위**로 이사했다(그 기둥 = 챕터 목록 · 가로 스플리터 · **[잠금 배너 + 챕터/편집 탭] 한 덩어리**. 목록 높이는 드래그로 정한다). ⚠ **행 스플리터의 뒤는 별(`*`) 행이어야 한다** — 처음엔 뒤가 배너의 `Auto`라 아래로 끌 때 줄일 것이 없어 **위로만 끌렸다**(2026-08-22 소유자 보고). 앞은 사람이 정하는 절대 높이(+하한), 뒤는 남는 자리를 받는 별. 자리만 그 뷰가 내주고 **내용은 셸이 짓는다**(`ChapterListHost`·`ChapterAddButton`·`ChapterSummaryText` 세 손잡이 — 목록 클릭이 세션 전환·엑셀 열기와 얽혀 있어 뷰로 옮기면 배선이 두 벌이 된다). 잃은 것: **챕터 목록이 챕터 그래프 탭에서만 보인다**(연출 그래프는 툴바 드롭다운, 무대 프리뷰는 씬 선택기가 그 일을 한다). **⚠ 노드 편집기 묶음 + 에셋 탐색기도 같은 날 `GraphEditorView` 안으로 들어갔다**(소유자: "오직 연출그래프에서만 보이도록 안에 넣어줄래") — 그 판의 Row 1이 `*,6,460`(그래프·스플리터·곁기둥)이고, **창은 `TabControl` 하나만 든다**(`CenterColumns`·`RightColumn`·`EditorPanel`이 전부 사라졌다). 편집기 넷은 XAML이 아니라 `MainWindow`의 필드이고 `GraphEditorView.SetSidePanel`로 건다 — 여기서도 **자리는 뷰의 것, 배선은 셸의 것**이다. **`ApplyTabChrome`은 이름이 겹치는 내보내기 단추 셋만 남았다**: 자리가 곧 소속이면 chrome이 할 일이 없다. **⚠ 잃은 것: 에셋 탐색기 → 무대 드래그** (둘이 동시에 안 보인다). 대체는 조절창의 [배경]·[캐릭터] 탭이고, 탐색기 [새로 고침]도 연출 그래프에서만 닿는다(열린 항목). **[편집 자료] 현황판은 2026-08-21에 삭제됐다**(소유자: "굳이 표시할 필요가 없는 것 같아" — 발행·배선이 자동이 된 뒤 볼 이유가 사라졌다). **우클릭은 소유자 보류.**
>
> **⚠ 성능 규칙 (2026-08-18)** — 노드 60개에서 첫 화면이 58초였다. 원인은 판 그리기가 아니라(그건 2.6ms) **같은 일을 여러 번** 한 것이다. 셋을 지킬 것: ① **`SetStatus`는 프로젝트 변경이 아니다** — `StatusChanged`로 따로 운다(상태 한 줄이 워크북 전체 재읽기를 부르던 고리를 끊었다) ② **재읽기는 `QueueReload()`로 합친다** — 변경 수십 개가 한 UI 차례에 한 번만 돈다 ③ **검증은 챕터별 (내용해시, 결과) 캐시** — 디스크가 그대로면 다시 증명하지 않고, 내보내기는 `ExportValidated`로 그 결과를 받아 쓴다(예전엔 같은 증명을 두 번 돌렸다). **툴이 워크북을 쓰면 `Report()`가 명시적으로 재읽기를 예약한다** — 예전엔 상태 메시지가 그 일을 우연히 대신했다. 결과: 첫 화면 58.3초 → 1.66초, 갱신 426 → 111ms. 고정은 시간이 아니라 **일의 횟수**로 건다(`ChapterGraphWorkAmountTests`).
>
> **⚠ 출력 규격 (2026-08-18)** — 이미터는 이제 **대본 하나**(`Story_{이름}.yarn`) + 폴더당 `declarations.yarn`만 낸다. `Pres_*`·`Set_*` 파일, `<<pres_start/end>>`, `<<beat Set_X>>`, 합성 추적 변수 `$__ch_N`, `[adv/]` 마커가 **전부 사라졌다** — 런타임이 레인·세이브·변수 저장소를 걷어냈고, **합치는 쪽이 이 도구**가 됐기 때문이다(소유자: "디버깅비용이 최소 10배 이상"). 연출 커맨드는 **노드 셋업은 대본 머리에, 줄에 붙은 것은 자기 대사 줄 바로 앞에** 인라인으로 선다. `#line:` 태그는 **계속 낸다**(런타임은 안 쓰지만 연출이 매달리는 열쇠다). **잃은 것 둘**: 줄 중간 연출(`[adv/]`), 메인 레인 전용 검사(`mainLaneOnly` 플래그가 이제 미사용). **조건 갈래(IF)의 커스텀 씬 출구는 `<<jump>>`가 아니라 `<<detour>>`다**(2026-08-21) — 씬을 재생하고 갈래로 돌아와 나머지 대본을 계속한다. ⚠ **프리뷰 재생도 2026-08-22부터 돌아온다**(그때까지는 나가는 절반만 있었다 — 소유자 보고): `PlaybackPath`가 `ExitOwnerLineId`를 내고 `spentBranchExits`를 받으며(다녀온 detour = 없는 출구 → 경로가 나머지로 이어진다), `StagePlayback`이 복귀 스택을 든다. 나가는 길은 `DetourReturnPlayback.Exit` 하나다. jump로 나가는 것은 선택지 옵션 출구와 기본 출구뿐(§A2-1). **커스텀 노드 자체에는 출구가 없다**(같은 날, §A2-2) — 복귀가 곧 출구라 기본 출구 포트가 커스텀 노드에서 폐지됐고(`DialogueNode.EffectiveDefaultExit` 한 곳이 규칙), 커스텀끼리 잇는 것도 조건 갈래(detour)의 몫이다. 구판 커스텀 배선 데이터는 지우지 않고 무시한다. 상세는 [`../runtime-contract.md`](../runtime-contract.md) §A.
>
> **대본 시트 규격 (v10, 2026-08-17)** — `episodes/{챕터}/{Id}.xlsx` **6열**:
> `인덱스 · LineId · 유형 · 조건라벨 · 화자 · 내용`. **태그·IN·OUT 폐지.**
> - `유형` = `대사`(빈칸도 같음) · `IF` · `ELSEIF` · `ENDIF` — 드롭다운. **CHOICE/OPTION은 규격에서 빠졌고**, 남아 있으면 리더가 그 행을 짚어 "선택지·간선 시트로" 말한다
> - **조건 분기는 블록이다** — `IF`~`ENDIF`가 감싸고 **중첩·ELSEIF 가능**. 구판의 §3.3 규칙 1·2·4·5·6(구간 대상·INPUT/OUT 짝·재사용 금지·중첩 금지·OUT 대조)이 **짝 하나로** 줄었다
> - `조건라벨`(D)은 **그 챕터의 `조건` 시트**에서, `화자`(E)는 **프로젝트 [화자] 탭**에서 오는 드롭다운(2026-08-23 — 화자만 챕터를 넘는다). `유형` 목록은 낱말이 파일에 굳으므로 동기화마다 대조해 갈아 끼운다
> - **인덱스 오름차순은 권고**다 — 읽는 순서는 시트의 행 순서이고 인덱스는 줄의 신원(연출이 매달리는 열쇠)일 뿐. 이행기가 구간을 옮기며 번호를 그대로 두는 것이 이 완화 덕이다
> - 구판 9열은 앱이 열 때 `EpisodeWorkbookMigrator`가 자동 이행(`.bak`)
>
> **줄이 전환 여럿을 든다 (2026-08-17)** — `DialogueLineExtension.Transitions` 목록. Yarn에는 전환만 있는 줄이 없어서 겹쳐 닫기·연달아 열기가 전부 다음 대사 줄 앞에 몰린다. 슬롯이 하나이던 시절에는 그래서 중첩도 인접도 못 썼다. **작가 판은 그게 무슨 조건인지 모른다** — 통과 화물로 들고 가서 합성기가 깊이 카운터로 되뱉을 뿐이다(소유자: "갈래로 보일 필요 없이 대본이 정한 대로")
>
> **시트 규격 (v11, 2026-08-18 · `화자` 폐지 2026-08-23)** — 챕터 워크북 **5시트**(+ 선택 `픽스처`):
> - `에피소드`: EpisodeId·제목·종류·대사엔트리·X·Y·메모 (+선택 `도달불가 허용` — **자리가 아니라 머리글 이름으로 찾는다**). **인덱스·표시조건·해금조건·선택지수·엔딩키 열 전부 폐지** — 나가는 간선 수가 곧 선택지 수이고, **엔딩키는 v11에서 간선으로 옮겨 갔다**
> - `간선`: 출발·도착·**스탯변화(C)**·**선택지(D = 문구 그 자체)**·**표시조건(E)**·**해금조건(F)**·잠금시 숨김·잠금 안내문·**종류(I)·엔딩키(J)·연출(K)**. **길 하나 = 선택지 하나**, 신원 = **(출발, 도착, 문구)** — 같은 곳으로 가되 스탯변화·관문이 다른 선택지 둘을 여럿 둘 수 있다. **관문은 전부 길의 것**(v8): 표시조건이 서야 보이고, 해금조건이 서야 고를 수 있다
>   - **v11의 새 세 칸은 직교한다** — `종류`(선택지/자동 = **누가 고르나**) · `엔딩키`(**여기서 끝나나**) · `연출`(**무엇을 재생하나**). 넷 조합이 전부 실재한다
>   - `종류`가 닫은 것: 예전엔 문구가 비면 자동 진행이라 *실수로 지운 것*과 *의도한 자동*이 데이터로 같았다. 이제 어긋나면 오류다
>   - **`엔딩키`는 툴이 안 쓴다**(기획자 칸). **`연출`은 툴이 짓고 툴이 적는다** — 엔딩키를 적고 저장하면 연출 그래프에 노드가 서고 이름이 이 칸에 되쓰인다. ⚠ 같은 도착으로 들어오는 간선들의 엔딩키가 다르면 **내보내기가 거부한다**(계약에서는 엔딩키가 도착 에피소드의 것이라, 조용히 하나를 고르면 나머지가 사라지고 JSON에 도착한 뒤엔 아무도 못 본다)
> - **`선택지` = 챕터 전체의 문구 사전** (v9, 2026-08-17 소유자: "인덱스를 가져오는 게 아니라 그냥 깡으로 대사만"): **인덱스·대본·메모**뿐이고 **출발이 없다**. 여기 한 번 적어 두면 **어떤 에피소드의 어떤 간선에서든 가져다 쓴다**. 간선은 인덱스가 아니라 **문구 자체**를 적으므로, 사전에서 행을 지워도 이미 쓰인 문구는 산다 — **어휘집이지 배선이 아니다**(`조건` 시트가 라벨↔식 사전인 것과 같은 자리)
> - **문구 없는 간선 = 보이지 않는 기본**(에피소드당 하나, 관문 금지 — 어떤 선택지도 안 될 때 빠지는 방어장치)
> - `조건`: 라벨·**스탯(드롭다운)·연산자(드롭다운)·값**·설명 — 연산자·값을 비우면 스탯 칸 전체가 원문 조건식(cleared:·복합식 탈출구). bool 조건은 연산자 칸이 true/false, 값 칸 회색
> - `스탯`: 스탯키·표시명·초기값·최소·최대·**타입(int/bool)**. bool은 경계 0·1 고정, **크기 비교 금지**(`Equal`만)
>   - **깃발은 증감이 아니라 지정이다** (2026-08-19): 간선 `스탯변화` 칸에 `met_willow true` / `false`. 정수는 지금대로 증감뿐이고(`trust +2`), **정수에 true/false도 깃발에 +1도 오류**다. 한 간선이 같은 깃발을 두 번 정해도 오류
>   - ⚠ **깃발을 쓰는 챕터는 `progression.json`이 안 나간다** — 진행 계약의 `StatChange`가 '정하기'를 실을 칸을 아직 안 가졌다. 빼고 내면 깃발이 영원히 안 켜진 채로 게임이 돌아가므로 거부한다. 저쪽에 칸이 서면 `ChapterProgressionExporter.BoolSetNotCarried` **한 함수만 지운다** (규격: [`../work-orders/bool-stat-orders.md`](../work-orders/bool-stat-orders.md))
> - **`스탯`은 2~5개, 범위는 0~5쯤으로 좁게** — 취향이 아니라 계산 한계다. 도달성 증명이 (에피소드 × 스탯 벡터)를 완전 탐색하므로 5개 × 0..100이면 100억이다. 넓혀 두었다 좁히면 이미 쓴 조건식이 전부 의미를 잃는다
> - ~~`화자`~~: **폐지 (2026-08-23)** — 챕터 엑셀의 어느 시트도 화자를 안 봤다(검증·증명·내보내기도). 등록은 툴 **[화자] 탭** → `game.definition.json`. 구판 시트는 앱이 열 때 한 번 옮겨지고 **사라진다**(`.bak`)
> - ~~`픽스처`~~: 임시 제거(있으면 읽음, 새로 안 만듦, UI 숨김)
>
> **전이 규칙 (v9)**: ① 고른 선택지의 간선(표시·해금조건이 서야 뜬다 → StatChanges 커밋) ② 못 고르면 **문구 없는 간선**(보이지 않는 기본) ③ 없으면 챕터 종료. **화면에 뜨는 순서 = `간선` 시트의 행 순서.**
>
> **툴 편집 탭 = 선택지 목록**: 줄 하나가 길 하나(`문구 → 도착 [조건]`). [＋]는 빈 폼 열기, 줄 클릭은 그 줄 아래에서 **문구·도착 고치기**(`[선택지 ▾] → [도착 ▾] [수정]`), ✕는 그 길 삭제(**사전의 문구는 남는다**). 문구 드롭다운은 **챕터의 모든 문구**를 담는다 — "자기 것만 고르는 게 아니라 자유자재로"(2026-08-17 소유자).
>
> **⚠ v8의 알려진 손실**: 들어오는 간선이 없는 에피소드(부착)의 관문은 옮길 곳이 없어 이행에서 사라진다. 부착의 표시 제어는 별도 규격이 필요하다(열린 항목).
> **대본 엑셀의 CHOICE/OPTION은 v10에서 규격에서 빠졌다** — 남아 있으면 리더가 그 행에서 오류로 짚는다.
> **⚠ 런타임 계약이 2026-08-18에 전면 개정됐다 — [`../runtime-contract.md`](../runtime-contract.md).** 런타임이 **세이브/로드 · 변수 저장소 · 서브 레인(Pres) · SubLane/BeatLane · 선택지 리플레이를 전부 걷어냈고**(실측 0건), **진행 계층은 `ked-progression`이라는 별도 순수 C# 패키지로 빠져나갔다** — VnTool과 유니티가 **같은 구현을 공유**한다. 그래서 계약 표면이 둘이다: 1부 `.yarn` 텍스트, 2부 `progression.json` → `Ked.Progression`. **`#line:` 태그는 더 이상 요구되지 않는다**(세이브가 없어졌다 — 다만 LineId는 우리 쪽 연출의 열쇠이므로 그대로 둔다). **`Stats[]`는 2026-08-18에 내보내기 시작했다**(§G-1 닫힘) — ⚠ 타입 이름을 번역한다: 이쪽 `Int`가 저쪽 `Number`다. **키는 PascalCase다.** 2026-08-18~19에 `EndingKey`(도착 에피소드로 번역)와 `ViaNodeId`(간선의 `연출`)도 나가기 시작했다 — **저쪽 DTO·로더·전이기·세이브도 전부 섰다.** 양쪽에 남은 미결은 §7이 진다.
> **구판 파일은 앱이 열 때 `ChapterWorkbookMigrator`가 자동 이행**(.bak 남김 — 구→v5→…→v11 한 번에). **겉모습(맑은 고딕 10 · 시트마다 다른 진한 머리글 · 얇은 회색 격자 · 고정·필터·열 너비)도 이행이 입힌다** — 만들 때와 같은 함수(`ChapterWorkbookWriter.ApplyChapterChrome`)를 부른다. 견본만 예쁘고 새 챕터는 밋밋하던 것이 이것 때문이었다.
> **대본은 챕터별 폴더 `episodes/{챕터}/`에 산다** (2026-08-16) — EpisodeId는 챕터 안에서만 유일하다. 뿌리에 평평하게 있던 구판 원고는 동기화가 그 챕터 폴더로 옮긴다(같은 이름을 여러 챕터가 쓰면 옮기지 않고 경고).
>
> **다른 폴더로 저장 = 프로젝트 이사** (2026-08-21) — [다른 이름으로…]가 새 폴더로 갈 때
> 규약 데이터(`chapters/`·`episodes/`·`game.definition.json`·상대 경로 에셋·튜닝)가 **함께
> 복사**된다(있는 파일 불가침·`~$` 배제·`exported/` 제외). 매니페스트만 옮기면 판·작가
> 화자는 따라가고 워크북·정의는 남아 "새 프로젝트에 이전 챕터가 보이는" 반쪽 프로젝트가
> 됐다(소유자 보고 — `AuthoringSession.CarryProjectData`).
> **편집의 기본은 엑셀이다** — 우측 위 "엑셀에서만 편집" 체크(기본 켬) = 툴 읽기 전용. 체크를 풀면 단추 없이 저장(Id는 Enter로 개명, [＋]로 선택지 폼 열기, 줄 클릭 = 그 아래 폼에서 문구·도착 고치기). 챕터 탭 조건 편집 UI 제거.
> **⚠ 엑셀이 그 챕터 파일을 열고 있으면 툴의 모든 쓰기가 거부된다.** 오른쪽 패널 위에 붉은 배너가 서고, 엑셀을 닫으면 스스로 걷힌다(2026-08-16 — "추가가 안 된다"의 정체).
> **화면 규칙 (2026-08-16 폴리싱)**: 왼쪽 챕터 목록은 **더블클릭**으로 엑셀을 연다(한 번 클릭 = 선택만) · 처음 보이는 탭은 **[편집]**이고 선택이 탭을 바꾸지 않는다 · **[에피소드 삭제]는 툴바 오른쪽 끝에 늘 있다**(평소 회색·비활성, 편집을 열면 빨강·활성 — 단추가 튀어나와 오조작하던 실사례) · **대사는 편집 탭의 선택지 아래 접이식**(별도 탭 폐지, 흐르는 글 — 줄마다 카드로 가르지 않는다) · 선택지 폼은 **한 줄**.
> **안 쓰는 사전 낱말은 정상** — 사전은 어휘집이므로 아무 길도 안 쓰는 문구가 남아 있어도 잘못이 아니다.
>
> 상세·근거·되돌리는 법: run-log 2026-08-16~19 항목들 (화자 시트 → v5 → UI 개편 →
> v6 → v7 → v8 → 챕터별 대본 격리 → 화면 폴리싱 → 엑셀 잠금 → v9 →
> **셸 재배치**(2026-08-18 팀장 미팅) → 성능 → 문서 정리 → 단일 대본 →
> **v11**(간선의 종류·엔딩키·연출) → 워크북 서식 → **깃발 지정** 순).

이 문서 하나로 "무엇을 왜 만들고 있고, 지금 어디까지 됐고, 다음에 무엇을 해야 하는지"가
서게 하는 것이 목적이다. 세부는 각 절이 가리키는 문서로 간다.

| 이 문서가 가리키는 곳 | 무엇이 있나 |
|---|---|
| [`../run-log.md`](../run-log.md) | **결정의 정본.** 항목마다 한 일·근거·되돌리는 법. 새 작업은 여기에 한 항목을 남긴다 |
| [`../work-orders/chapter-graph-orders.md`](../work-orders/chapter-graph-orders.md) | 규격 원본 — 엑셀 시트·열, 기능 G1–G9, 전이 규칙 |
| [`../chapter-layer-guide.md`](../chapter-layer-guide.md) | 기획자용 사용 안내 (챕터 그래프) |
| [`../runtime-contract.md`](../runtime-contract.md) | **런타임과의 계약 (2026-08-18 전면 개정).** 1부 `.yarn` · 2부 `progression.json` → `Ked.Progression` · 3부 열린 항목 |
| [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md) | 코드 구조 정본 — "이 기능은 어느 파일" 표가 여기 있다 |

진행 상태(닫힌 것·열린 것)는 이 문서 **§7**이 진다 — 예전에는 `episode-layer-master-plan.md`가
"최상위 기준 문서"를 자처해 이 문서와 둘이 같은 자리를 다퉜다. 2026-08-18에 그쪽을 접었다.

---

## 1. 무엇을 만드는가

**VnTool** — 비주얼 노벨 저작 도구. C#/.NET 10 + Avalonia, 한국어 코드베이스.
프로젝트 루트 `C:\Users\river\Documents\GitHub\java-start`, 솔루션 `VnTool.sln`.

만들어 내는 것은 유니티 런타임(별도 저장소)이 먹는 데이터다:
`exported/{챕터}.progression.json` + Yarn 번들.

### 목적 — 왜 이 구조인가

**세 사람이 서로를 막지 않고 동시에 일하게 하는 것.** 그래서 층을 갈랐다:

```
A 계층 (기획자)          B 계층 (작가)             연출 계층
챕터 그래프 · 엑셀        연출 그래프 · 엑셀           연출 노드
에피소드 구조             대사 한 줄 한 줄            커맨드·무대
간선 = 행선지 + 스탯변화   에피소드 안의 조건·연출     발행된 결과 위에서만
```

**각 값의 주인이 한 곳뿐이어야 한다**가 설계 전체를 관통하는 규칙이다. 같은 사실이
두 곳에 있으면 어긋나고, 어긋남은 최종 출력에서야 드러난다.

---

## 2. 계층 의미론 — 확정된 규칙 (건드리지 말 것)

### 2.1 소유권

| 값 | 주인 | 툴에서는 |
|---|---|---|
| 대사 본문·화자 | 에피소드 워크북 **`episodes/{챕터}/{Id}.xlsx`** (2026-08-16 — 챕터별 격리) | **읽기 전용.** 엑셀노드 카드는 잠긴다. EpisodeId는 챕터 안에서만 유일하므로 폴더가 챕터로 갈린다 |
| 에피소드 구조·간선 | 챕터 워크북 `chapters/{Id}.xlsx` | 툴 편집이 **엑셀 셀에 즉시 저장**된다 |
| 스탯 변화 | **간선 하나뿐** (`간선` 시트 C열 `스탯변화`) | 대본에서 스탯 조작 = 설계 미스, 폐지됨 |
| 선택지 문구 | 챕터 **`선택지` 시트 = 전역 사전** (2026-08-17 v9) | 어느 에피소드의 것도 아니다. 간선이 그 문구를 **직접 적어** 쓰고, 순서는 `간선` 시트의 행 순서다. 대본 CHOICE/OPTION 폐지 수순 |
| A계층 조건 식 | 챕터 `조건` 시트 | 작가의 **고르기 목록에서 빠진다** — 이미 쓰인 것만 `[기획] 라벨`로 읽힌다 (§2.4) |
| 에피소드 안의 조건·연출 | 연출 그래프의 자유 노드 | 작가가 직접 만든다 |
| 줄 신원(LineId) | 프로젝트의 `ExcelLineMap` | B열은 유물. 툴은 워크북에 **쓰지 않는다** (v4) |
| 화자 등록 | **툴 [화자] 탭 → `game.definition.json`** (2026-08-23 — 챕터 `화자` 시트 폐지) | **엑셀이 아니라 툴이 주인인 유일한 기획 데이터.** 챕터 엑셀이 화자를 전혀 안 썼기 때문이다. **모든 챕터가 한 목록을 공유**하고, 그 이름이 **모든 챕터**의 대본 E열 드롭다운으로 밀린다. **v4의 유일한 예외**: 툴이 원고 파일의 숨김 `화자목록` 시트만 갈아 끼운다(대사 행 불가침, 목록 같으면 무접촉). 설정노드·대사노드는 **읽기만** 한다 |
| 초상화 매핑 | `game.definition.json`의 speakers — **화자 이름과 같은 배열** | 이름과 캐릭터키가 한 집에 산다(2026-08-23). 합쳐 볼 둘째 원천이 없다 |

### 2.2 전이 규칙 (v9, 2026-08-17 — 맨 위 박스가 정본)

에피소드가 끝나면:

1. 플레이어가 고른 **선택지의 간선**을 탄다 → `StatChanges` 커밋(clamp) 후 이동
2. 아무 선택지도 못 고르면(조건 전부 미달·보이는 길 없음) **문구 없는 간선**
   (보이지 않는 기본 — 관문 없는 방어장치)
3. 그 외에는 **챕터 런이 여기서 끝난다**

2026-08-15의 "낙하 폐지"는 이 형태로 개정됐다 — 기본은 낙하가 아니라 **명시된 방어장치**다.

### 2.2b 관문은 길의 것이다 (v8, 2026-08-16)

표시조건(보이려면)·해금조건(고를 수 있으려면)은 **간선**이 갖는다. 에피소드 시트에는
그 두 열이 없다. 판정은 한 곳에서 끝난다 — 프루버·픽스처 워커도 간선 관문만 본다.

### 2.3 간선 신원 = (출발, 도착, 문구)

**길 하나가 곧 선택지 하나**다(v9). 같은 도착으로 문구를 여럿 두는 것은 흔한 패턴이라
그대로 허용된다 — 문구가 다르면 다른 길이고, 스탯변화·관문을 따로 건다. 문구의 원천은
챕터 `선택지` 시트의 **전역 사전**이지만, 간선 셀에 들어가는 값은 인덱스가 아니라 **문구
자체**다: 사전은 고르기 편하라고 있는 어휘집이지 배선이 아니다. 그래서 사전에서 낱말을
지워도 쓰이던 문구는 살아 있고, 아무 길도 안 쓰는 낱말이 남아 있어도 잘못이 아니다.

같은 에피소드에서 **같은 문구가 여러 갈래로** 가면 경고한다 — 플레이어에게는 같은 버튼
둘로 보인다(막지는 않는다: 사람이 판단할 일이다).

### 2.4 A 계층 격리 (2026-08-15 → **2026-08-17 확장**)

**두 계층은 서로 완전히 다르다**(2026-08-17 소유자) — 시나리오 설정노드의 조건·변수·화자와
챕터 엑셀의 그것은 이름이 같아도 다른 것이다. 세 곳에서 갈린다:

| | A계층 (기획자) | 작가 화면에서는 |
|---|---|---|
| **조건** | 챕터 `조건` 시트 → 공급 설정노드 | **고르는 목록에서 빠진다.** 이미 쓰인 것은 `[기획] 신뢰높음`으로 이름이 보인다(숨겨서 "알 수 없는 조건"이 되면 더 나쁘다) |
| **변수** | 챕터 `스탯` 시트 (`trust`·`anger`) | **정의 파일에서 후보를 꺼내오지 않는다** (2026-08-17 — 거르는 것으로는 부족하다). 작가가 쓰는 것은 **아이템·능력**이고 후보는 이 챕터에 이미 있는 이름들뿐이다 |
| **화자** | 툴 **[화자] 탭** → 정의 파일 speakers (2026-08-23) | 조건노드에 **회색 고정**으로 선다(수정 불가). 표정은 누구든 자유롭게 — 표정의 주인은 에셋 폴더 규약이다 |

동기화가 만드는 `챕터 {판이름} 조건` 설정노드는 **카드도 공급 링크도 그려지지 않는다**
(`GraphProjectionBuilder`). **계층을 가르는 규칙은 `EpisodeSyncService.IsConditionSupplyNode`
하나뿐이다** — 그래프의 카드 숨김도, 조건 목록의 배제도 같은 것을 부른다(사본 금지).

A계층 어휘(**스탯 키**)는 **챕터 그래프가 읽은 목록 하나**에서 세션으로 온다
(`AuthoringSession.SupplyChapterVocabulary`) — 두 곳이 따로 xlsx를 읽지 않는다.
⚠ **화자는 2026-08-23에 여기서 빠졌다** — 챕터에서 오는 화자라는 것이 없어졌고, 원천은
`Definition.Speakers` 하나다.

**단, 데이터로는 살아 있다** — 엑셀 `조건라벨` ↔ 식 역조회의 신원 저장소라 지우면
동기화가 무너진다.

### 2.4b `game.definition.json`은 기획자 전용이다 (2026-08-17)

정의 파일이 담는 것: **variables(A계층 스탯) · conditions · events · speakers(초상화 매핑)
· 연출 커맨드 카탈로그**. 작가의 자료는 하나도 없다.

**툴에서 정의 파일에 쓰는 창구는 둘**이고, **둘 다 기획자 화면(챕터 그래프)에서만** 불린다:
검증 보고의 **[등록]**(변수)과 **[화자] 탭**(speakers). 작가 화면이 speakers를 갈아 끼우던
길은 폐지됐다.

⚠ **화자의 주인은 기획자 하나이고, 그 자리는 툴이다** (2026-08-23 소유자: *"엑셀 내 어떤
것에서도 화자를 사용하지 않는다. 즉 애초부터 챕터엑셀에 화자가 들어갈 이유가 전혀없다.
화자는 툴 내부에서, 직접 정의해서 쓰는 게 맞는 것이였다."*) — **챕터 그래프 우측
[화자] 탭**에서 추가·삭제하고, 값은 `game.definition.json`의 speakers에 산다.
**모든 챕터가 한 목록을 공유**하고, 그 목록이 **프로젝트의 모든 대본**에 드롭다운으로 밀린다.
**작가 계층에는 화자를 만드는 자리가 없다**: 설정노드는 회색 고정 목록만 보이고(표정 단추는
남는다), 대사노드 드롭다운은 **한 구역**이다. 없어도 대본은 돈다: 화자 칸은 자유 입력이고
목록은 편의일 뿐이다.

⚠ **왜 이것만 엑셀이 아닌가** — 챕터 워크북의 어느 시트도 `화자`를 참조하지 않았다
(`조건` 시트는 간선의 표시조건·해금조건이 실제로 가리킨다 — 그래서 그쪽은 챕터의 것이다).
검증도, 도달성 증명도, 내보내기도 화자를 안 본다. 툴이 대본 드롭다운에 쓰려고 남의 파일에
얹어 둔 사전이었고, 그 때문에 챕터마다 따로 적어야 했다. 하루 앞서 그 값을 **합집합**으로
기우려던 시도가 있었는데(2026-08-23 오전), 잘못된 자리를 유지하는 비용만 늘리는 일이라
소유자 판단으로 통째 되돌렸다.

⚠ **이행** — 구판 챕터의 `화자` 시트는 앱이 열 때 한 번 정의 파일로 옮겨지고 **시트가
사라진다**(원본은 `.bak`). **순서가 규격이다**: 시트를 지우는 데 성공한 뒤에 정의 파일에
쓴다 — 반대로 하면 엑셀이 잡고 있어 못 지운 워크북이 다음 재읽기에서 **사람이 방금 지운
이름을 되살린다**.

⚠⚠ **`StoryProject.WriterSpeakers`는 유물로 남는다** — 아무도 읽지 않지만 지우지 않는다
(구판 프로젝트 파일의 값을 다음 저장에 조용히 날리지 않으려고). 하루 살았던
`WriterSpeakerPush`(작가 화자 → 챕터 시트 자동 반영, 2026-08-22)도 함께 걷혔다 —
넣을 화자가 없어지면 부를 자리도 없다. **이미 시트에 적힌 이름은 남는다**(이제 기획자의 것).

### 2.4c 설정노드 = 챕터의 컨트롤러 (2026-08-17)

**챕터마다 설정노드 하나가 자동으로 서고, 만들거나 지우지 않는다** (소유자: "조건 노드라기
보다는 챕터별로 자동으로 저장되는 컨트롤러에 가까운 것 같아"). 공급 범위가 판 전체가 된
뒤로는 여러 개일 이유가 없다 — 어차피 다 같은 곳에 미치므로 나눠 놓으면 "어디에 적었더라"만
생긴다. `ProjectEditor.EnsureChapterSettingsNode`(이름 `{챕터} 설정`)를 판 여는 길에서
보장하므로 구판 프로젝트도 열면 생긴다. **[+ 설정 노드] 폐지 · 삭제 차단**(안의 것은
노드를 열어 하나씩 지운다). A계층 공급 노드는 별개 배관이라 세지 않는다.

**그 챕터에서 작가가 쓰는 것들이 여기 모인다** — **조건 · 아이템·능력 · 화자**.

**조건은 세 칸이다** (2026-08-17 소유자 — "대사노드에서 Set을 다루는 감각과 동일하게"):
`[이름] [아이템·능력 ▾] [부호 ▾] [수치]`. 식을 손으로 쓰지 않는다 — 작가가 Yarn 문법을
알아야 할 이유가 없다. 챕터 `조건` 시트와 **같은 분해기 하나**를 쓴다(`$`만 떼고 넣는다).
**능력에는 부호가 없다**: 조건은 On/Off 토글(식은 `== true/false`), 대사 줄 Set은 연산자
콤보를 감추고 `=`로 고정한다 — 고를 것이 하나뿐이면 고르게 하지 않는다.
손으로 적은 복합식은 **분해하지 않고 읽기 전용으로 지킨다**.

**작가의 "변수"는 아이템·능력이다** (2026-08-17 소유자: "시나리오 작가가 쓰는건 변수라기
보다는 아이템이라고 하는게 맞겠다. 아이템 혹은 능력"). 종류 드롭다운이 자료형 이름
(`float`/`bool`)이 아니라 **`아이템 (개수)` · `능력 (보유)`**로 뜻을 말한다 — 아이템은
개수로 늘고 줄고, 능력은 있다/없다뿐이다. **저장되는 자료형은 그대로**라 옛 프로젝트도 읽힌다.

**⚠ 아이템·능력은 오직 챕터 단위로만 산다** (소유자: "짧은 스토리 단위를 상정한거야 …
그게 이렇게 널부러지면 곤란해"). Yarn의 변수 저장소는 하나뿐이라(계약서 D1) 그냥 두면
1챕터의 `$열쇠`와 3챕터의 `$열쇠`가 같은 변수가 된다. 그래서 이미터가 **챕터 네임스페이스**를
붙인다 (`Tier1Namespace`, `chapter-graph-orders.md` §0.5 구멍 A의 해법):

- 접두는 **`__t1_{판Id}_`** — 뿌리가 판 Id라 **챕터 이름을 바꿔도 아이템이 초기화되지 않는다**
- **선언·set·조건식** 세 곳에 일관되게 붙는다(하나만 빠져도 컴파일이 깨진다)
- **A계층 스탯에는 안 붙는다** — 챕터를 넘어 사는 게 맞고 런타임 브리지가 이름 그대로
  왕복한다. 무엇이 스탯인지는 **추측하지 않는다**: 챕터 조건 공급 노드가 쓰는 이름만 스탯이다
- 합성 추적 변수(`$__ch_N`)도 그대로 — 노드 안에서 쓰고 버린다
- 덕분에 **작가가 아이템을 `trust`라 지어도 스탯을 건드리지 못한다** — 구멍 A가 구조적으로 닫혔다

**개별 Settings link도 폐지됐다.** 판 = 챕터 1:1이므로 **같은 판에 서 있는 설정노드는 그
판의 모든 대사노드에 자동으로 미친다**(`ConnectedSetNodeResolver`). 노드를 만들 때마다
배관을 다시 잇지 않는다.

- **그래프에 조건 공급선을 그리지 않는다** — 범위가 판 전체라 선을 그리면 노드 수만큼
  거미줄이 되고, 그 선이 무엇을 정하지도 않는다. ⚠ **SetNode 카드에는 포트가 하나도
  없다** (2026-08-22 소유자) — "이 챕터에 공급 중" 포트가 사실 표시로만 남아 있었는데,
  **카드에 붙은 포트는 그 자체가 "여기서 이을 수 있다"는 말**이라 이을 수 없는 포트는
  거짓말이었다. 끌어 놓기 처리(`HandleSupplyDrop`)도 함께 죽었다 — 링크 데이터와
  `AddSettingsLink`는 그대로 산다
- 구판 프로젝트의 `Links` 데이터는 지우지 않고 **조용히 무시**한다
- 순서는 **판 안의 노드 순서**(옛 링크 Order를 대신한다)
- **"사용할 수 없는 조건" = 다른 챕터에 있다** — 조건이 사라진 것(`UnknownCondition`)과
  다른 챕터에 있는 것(`UnavailableCondition`)이 갈린다

> **조건노드(SetNode) 자체는 작가의 도구다.** 에피소드 안에서만 쓰는 B계층 변수·조건은
> 작가가 직접 만들어 쓴다. 금지된 것은 "A계층 데이터를 그 자리에 부어 놓는 것"뿐이다.

### 2.5 출구 규칙

- **커스텀(자유) 노드에는 출구가 없다** (2026-08-21 소유자) — detour로 재생되고 끝나면
  호출한 갈래로 **돌아가는 것**이 곧 출구다. 커스텀끼리 잇는 것도 씬 안의 조건 갈래
  (detour)가 맡는다. 규칙은 `DialogueNode.EffectiveDefaultExit` 한 곳, 구판 배선
  데이터는 지우지 않고 무시
- 갈래 출구 후보에 **엑셀노드는 없다** (기획의 영역으로 넘어가는 선은 금지)
- 커스텀 노드가 배선에서 빠지면 **(진행)** = 에피소드 종료 후 챕터 간선이 정한다
- 엑셀노드의 옵션 포트·기본 출구는 카드가 아니라 **철도 칩**에 있다 (T2) —
  기본 출구를 가질 수 있는 것은 엑셀노드뿐이다
- ⚠ **대사 편집기의 [기본 출구] 구역은 2026-08-22에 삭제됐다** (소유자: "연출그래프에서
  만질 일이 없을 테니") — 같은 값을 레일 칩(`○ 진행`)이 이미 편집해 **창구가 둘**이었다.
  **지운 것은 편집 창구뿐이다**: `DefaultExitTargetNodeId`·`EffectiveDefaultExit`·
  `ExitPortKind.Default` 포트·발행 점프·검증은 그대로이고 **출력이 안 바뀐다.**
  데이터·출력에서까지 걷으려면 계약 §A2-1을 함께 봐야 한다(안 했다)

---

## 3. 두 화면

### 3.1 챕터 그래프 (기획자)

- 카드 = 에피소드. **선택지 포트가 카드 오른쪽**에 뚫린다 — 원천은 **문구가 붙은 나가는
  간선**(v9). 포트 하나 = 길 하나이고 누르면 그 간선이 선택된다. 문구 없는 간선(보이지 않는
  기본)은 포트가 아니라 카드 중앙에서 직결.
- 카드에 **도착 스탯 줄**(2026-08-17) — `신뢰 1~3`. 그 노드에 **들어오는 간선의 증감까지
  커밋한 뒤**의 값이고, 들어오는 길이 여럿이면 최소~최대로 벌어진다. 값은 도달성 증명이
  걷는 그 걸음에서 받아 적으므로 화면과 증명이 어긋날 수 없다(`SpansFor`). 챕터에서 한 번도
  안 움직이는 스탯은 카드에서 뺀다(단 **아무것도 안 움직이는 챕터**에서는 거르지 않고
  초기값을 보여 준다 — 안 그러면 기능이 없는 것처럼 읽힌다) · bool은 참/거짓 · 탐색이
  상한에서 잘리면 `≈`
- **스탯변화를 툴에서 적는다** (2026-08-17) — `[스탯 ▾][＋/－][수치][✕]` 줄 편집기
  (`StatChangeEditor`). 자리 둘: 간선 패널, 그리고 **에피소드 노드의 선택지 폼**(배선과
  증감이 한 저장으로 나간다). 값이 사는 곳은 그대로 `간선` 시트의 `스탯변화` 칸 하나다
- **휠 확대·축소 / 가운데 단추 끌어 이동** (2026-08-17, Ctrl 요구는 2026-08-18 해제) — 연출 그래프와 같은
  손놀림·같은 산식. 배율은 `ZoomHost`(LayoutTransformControl)가 지므로 캔버스 좌표
  (배치·간선 산식)는 배율을 모른다. 100%가 아닐 때만 오른쪽 아래에 배율 단추
- **챕터를 고르는 길은 `SelectChapter` 하나다** (2026-08-17) — 왼쪽 목록 클릭·위 드롭다운·
  코드가 전부 이 길을 지난다. 검증 재실행 · 이전 선택 비우기 · `ChapterSelected` 알림이
  거기 모여 있다. 드롭다운이 `Draw()`만 부르던 것이 "Ctrl+S를 눌러야 빨간 테두리가
  사라진다"와 "왼쪽 목록이 안 따라온다"의 <b>같은</b> 뿌리였다
- **엑셀이 그 챕터를 잡고 있으면 [엑셀에서만 편집]을 풀 수 없다** (2026-08-17) — 잠금 중
  체크는 강제 켬 + 비활성. 편집 중에 엑셀이 열리면 **열려 있던 폼의 값을 먼저 내고** 잠근다.
  풀리면 툴이 스스로 켠 것만 되돌린다. 검증용 `LockProbe` 손잡이가 있다(진짜 잠긴 파일은
  쓸 수도 없어 "잠기기 직전 저장"을 실제 잠금으로는 검증할 수 없다)
- **간선 패널은 [적용]이 없다** (2026-08-17) — 고르는 순간 저장. 글자 칸(잠금 안내문·스탯
  수치)만 초점을 잃거나 Enter일 때. 저장 뒤 `Reload()`가 필수다: 문구가 간선의 신원이라
  한 번 고치면 손에 든 모델이 낡고, 그 상태의 다음 저장은 간선을 못 찾는다
- 탭 둘: **[편집]**(처음 보이는 탭 — Id 개명·선택지 목록·[＋]/줄 클릭 폼·접이식 대사,
  길을 고르면 표시·해금조건·잠금·스탯변화) · **[챕터]**(조건·스탯 읽기 전용).
  선택이 탭을 바꾸지 않는다. 삭제는 툴바 오른쪽 끝 빨간 단추
- 아래 검증 보고 — **동기화 보고가 맨 위**(묻혀서 안 보이던 실사례)
- **진행 JSON은 저절로 나간다** (2026-08-17, [내보내기] 단추 폐지) — 다시 읽을 때마다
  **모든** 챕터를 `exported/`에 쓴다(같은 글이면 안 쓴다). G8 거부 규칙 그대로: 검증
  오류가 있으면 그 챕터 파일은 손대지 않고, 검증 보고 맨 위 + 머리글 `· 진행 JSON 미출력`.
  상태줄은 안 쓴다 — 동기화 보고가 곧 덮는다(실제로 덮였다)

### 3.2 연출 그래프 (작가) — 철도 배선 · 옛 "시나리오 그래프", 기능 동결(2026-08-18)

> **⚠ 발행·배선은 2026-08-21에 자동이 됐다** — 연출 노드 카드·발행 결과/공급 끌기·
> [발행] 버튼들이 전부 사라졌다(소유자: "엄청 불필요한 것 같아 … 미리 다 해둔 다음에
> 무대 프리뷰측에서 뭘 할지 고르도록"). 연출의 입구는 **무대 프리뷰의 씬 선택기**이고,
> 고르면 `ProjectEditor.EnsurePresentationChannel`이 대사 발행 → 연출 노드 → 입력 고정 →
> 공급 연결 → 연출 발행을 한 번에 한다(멱등 · 내용 같으면 버전 불변 · binding 불가침).
> 연출 노드는 조건 공급 노드와 같은 **배관**으로 데이터에만 산다.
>
> **⚠ 연출 공급 노드도 같은 날 판에서 걷혔다** — [+ 연출 공급]·필터의 [공급] 체크란·
> 카드·간선이 사라졌다(잇던 상대인 연출 노드가 숨어 할 일이 없다). 커맨드 범위·프리셋
> 해석(`AvailablePresentationCommandResolver`)은 그대로지만 **편집 화면은 삭제됐다**
> (`CommandSupplyNodeEditor` — 되살리려면 그 커밋의 두 파일 + XAML 한 줄).
> **필터는 대사·설정 둘뿐이다** — **[흐름만] 단추는 2026-08-22에 폐지됐다**(체크 둘을
> 코드가 대신 눌러 주던 단축인데, 토글이 둘뿐이면 [설정] 하나 끄는 것보다 손이 더 갔다).
> 이 판에 남은 것은 스토리 흐름(레일·칩·갈래 배선)과
> 대사 노드 만들기·조건 편집이다.

챕터 간선이 작가의 판에 **오렌지 철도**로 미러된다. 규칙 하나: **한 구간에 선 하나.**

```
[EP01] ●선택지 ──▶[씬A]──(작가 배선)──▶[씬B]────●──▶[EP02]
        레일: 입구까지     속: 배선 하나      합류선: 레인 끝으로
```

- **가지의 주인은 챕터 `간선` 시트다** (v9, 2026-08-17) — 가지 하나 = 간선 하나. 대본에
  OPTION이 없는 것이 정상이라 **"유령 간선"이라는 개념이 없다**(그 경고가 v9에서 모든 간선에
  떴다 — 소유자 보고). 반대로 대본에만 남은 구판 OPTION 줄이 스텁이 된다
- **칩**(`● 라루를 믿는다` / `○ 진행`)이 배선 진입점. 누르면 상세 + 자유 씬 배선 플라이아웃
- 가지 순서 = **읽는 순서**(문구 있는 길 = 간선 시트 행 순서 → 구판 OPTION 스텁 → 진행)
- **자유 씬 배선의 열쇠는 문구**다 — `DialogueNode.ChoiceExits`(판 파일의 `choiceExits`),
  `ExitPortKind.Choice`. 대본 줄에 매이지 않아 대본을 고쳐도 살아 있다.
  ⚠ **아직 내보내기에 실리지 않는다** — 발행 점프가 줄에 매여 있다.
  ⚠⚠ **v11의 `ViaNodeId`와 헷갈리지 말 것**: 그쪽은 **챕터 간선의 `연출` 칸**이 원천이고
  2026-08-19부터 나간다. 이 `ChoiceExits`는 **작가 계층에서 선택지 문구를 열쇠로 매단 자유
  씬**이고 여전히 안 나간다 — 계약의 같은 칸으로 보낼지, 별도 채널을 둘지는 안 정했다
- 레일은 **첫 씬 입구까지만** 댄다. 커스텀 노드에 출구가 없어져(2026-08-21) 기본 배선
  체인은 사라졌고, 웹은 첫 씬 + 그 씬의 조건 갈래(detour) 씬들이다
- 배선된 씬(기본 출구 없음 = 진행)에서 **합류선**이 레인 끝으로 모인다
- 스텁: `⏹ 종료`(길이 여기서 끝남) · `▢ {Id} (동기화 전)`(간선은 있는데 노드가 아직 없음)
- 도착 포트 — 여러 가지가 도착 카드 앞 접점 하나로 수렴. 누르면 들어오는 길 목록

### 3.3 노드 시각 체계 (2026-08-15)

색 하나가 아니라 **등뼈(왼쪽 색 막대) + 아이콘 + 카드 형태** 세 채널을 겹친다.
정의는 `GraphEditorView.CardStyleFor` 한 곳. 미니맵·접힌 목록도 같은 언어를 쓴다.

| 종 | 등뼈 | 형태 | 아이콘 |
|---|---|---|---|
| 엑셀노드 | 호박 | **미색 종이, 각진 모서리(4)** | 📄 |
| 자유 씬 | 청록 | 흰 종이, 둥근 모서리(10) | ✎ |
| 설정(조건) | 파랑 | 옅은 파랑 | ⚙ |
| ~~연출~~ | 보라 | 옅은 보라 | 🎬 |
| ~~연출 공급~~ | 초록 | 옅은 초록 | 🧰 |

⚠ 아래 둘은 **2026-08-21에 판에서 사라졌다**(배관 숨김) — 카드 규칙은 코드에 남아 있지만
그리지 않는다. 무대 프리뷰 씬 선택기는 같은 언어를 쓴다(📄 에피소드 · ✎ 커스텀 씬).

---

## 4. 코드 지도 (이 층에서 자주 여는 곳)

| 하려는 일 | 파일 |
|---|---|
| 연출 그래프 철도·카드·미니맵 | `src/Vn.App/Views/GraphEditorView.axaml.cs` (`DrawChapterRails`·`DrawRailsFrom`·`DrawRailBranch`·`DrawAdvanceReturns`·`CardStyleFor`) |
| 챕터 그래프 화면·포트·편집 패널 | `src/Vn.App/Views/ChapterGraphView.axaml{,.cs}` (`DrawOptionPorts`·`PortY`·`ApplyEdgeFromPanel`·`RenameSelectedEpisode`) |
| 그래프에 무엇이 그려지는가 | `src/Vn.Authoring/Graph/GraphProjectionBuilder.cs` — 화면이 아니라 **여기가 정한다** |
| 엑셀 → 노드 동기화 | `src/Vn.Authoring/Chapters/EpisodeSyncService.cs` (`Sync`·`SupplyChapterConditionsToBoard`·`ConditionSupplyNodeName`) |
| **동기화가 언제 도는가** | `ChapterGraphView.WatchAndReload` — 챕터 선택 · 대본 폴더 변경 · **에피소드 목록 변경**(`EpisodeSetChanged`, 2026-08-22). ⚠ 재읽기마다 돌리지 않는다: `SyncEpisodes`가 에피소드 워크북을 전부 열어 보므로 저장 한 번마다 그 값을 치르면 §성능 규칙이 무너진다 |
| 챕터 워크북 읽기·쓰기 | `Chapters/ChapterWorkbookReader.cs` · `ChapterWorkbookWriter.cs` |
| 구판 워크북 자동 이행 | `Chapters/ChapterWorkbookMigrator.cs` — 구→v5→v6→v7→v8→v9 한 번에, .bak. 앱 Reload가 챕터 폴더 전체에 돌린다 |
| 대본 파일 자리·입양 | `Chapters/EpisodeLibrary.cs` (`FolderFor(project, chapterId)` = `episodes/{챕터}/` · `AdoptFlatWorkbook`) |
| 조건식 분해·조립·연산자 | `Chapters/ConditionExpressionParser.cs` (`TryDecomposeSingle` — 시트 세 칸 ↔ 식 문자열) |
| 검증·도달성 증명 | `Chapters/ChapterValidator.cs` · `ChapterReachabilityProver.cs` |
| 에피소드 워크북 생성·개명 | `Chapters/EpisodeLibrary.cs` |
| 모델을 바꾸는 유일한 통로 | `src/Vn.Authoring/Editing/ProjectEditor.cs` |
| 연출 채널 자동화(발행·배선) | `Editing/ProjectEditor.Results.cs` (`EnsurePresentationChannel`) — 입구는 `src/Vn.App/Views/MiniStagePreview.axaml.cs`의 씬 선택기(`SceneCombo`) |
| 무대 조절창(탭·칩·직접 조작) | `src/Vn.App/Views/StageSceneView.cs` (`BuildStagePopover`·`BuildQuickTab`·`BuildCharacterTab`·`ApplyStageCommand`) |
| 자주 쓰는 칩의 기본 목록 | `src/Vn.Authoring/Model/StageQuickCommand.cs` (`StageQuickCommands.Default`) — 담기·빼기는 `ProjectEditor.PinQuickCommand`/`RemoveQuickCommandAt` |
| 출구 포트 계산 | `src/Vn.Authoring/Flow/NodeConnections.cs` |

전체 구조와 "이 기능은 어디" 표는 [`ARCHITECTURE.md`](../../ARCHITECTURE.md).

---

## 5. 작업 관습 (반드시 지킬 것)

1. **모든 동작 변경에 run-log 항목 + 고정 테스트.** run-log는 커밋 메시지보다 상세한
   결정 기록이다. 소유자가 "왜 이렇게 됐나"를 여기서 따라간다.
2. **앱이 실행 중이면 빌드가 잠긴다.** `Vn.App.exe` 잠금 오류가 나면 **절대 강제 종료하지
   말고** 소유자에게 앱을 닫아 달라고 요청한다.
3. **PowerShell 5.1** — 커밋 메시지에 큰따옴표를 쓰지 말고 단일 인용 here-string(`@'...'@`)을
   쓴다. `&&`·삼항 연산자 없다.
4. 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
5. **실데이터로 재현한다.** 소유자 보고("~가 안 보인다")는 `DevSync\test3`를 임시 폴더로
   복사해 헤드리스로 그려 보면 대부분 즉시 재현된다(스크래치 테스트 후 삭제).
6. 소유자의 판단이 내 분석과 어긋나면 **소유자 판단을 참으로** 두되, 모순·오류는 지적한다
   (2026-08-14 소유자 지시).

---

## 6. 소유자의 실데이터

`C:\Users\river\Documents\DevSync\test3` — 실제로 쓰고 있는 프로젝트.

- 챕터 `3_ch00`: 에피소드 7개(시작에피소드 · EP01 · Ep02_A · Ep02_B · 엔딩 · new01 · rrr)
- 시작에피소드의 선택지 둘(`라루를 믿는다` → new01, `손을 잡는다` → rrr)
- **알려진 정리거리**:
  - `new01.xlsx`·`rrr.xlsx`는 대사 한 줄짜리라 판에 노드가 안 선다
    ("대사 0줄 워크북은 노드를 만들지 않는다" 규칙 — 대사를 쓰고 저장하면 선다)
  - `new02.xlsx`는 개명 사고의 잔재(원고 "t/w"). 살릴 것 없으면 삭제
  - EP01은 챕터에서 지워졌는데 판에 노드가 남아 있다 — 레일은 안 그려진다(정상)

---

## 7. 진행 상태

### 닫힌 것

- **Gate A·B·C 전부** — 리더/모델, 대본→노드 동기화, 검증기+도달성 증명, 내보내기
- **챕터 v2/v3/v4** — 엑셀 소유권 확정, 왼쪽 패널 = 챕터 목록, 툴은 워크북에 안 쓴다
- **2단계 무대(작가 계층)** — 엑셀노드 잠금, 포트 개통, 조건 자동 공급, 챕터 박스, 접기
- **시나리오 타임라인 T1·T2** — 철도 배선 + 포트 이사. **T3(자동 배치)은 철회**
  (소유자: "굉장히 어색하다"). 코드는 `5841eac`에 보존, 되돌린 커밋 `49be938`
- **v11 (2026-08-18)** — 간선이 `종류`·`엔딩키`·`연출`을 든다. 엔딩 간선에 연출 노드가
  저절로 서고 이름이 워크북에 되쓰인다. 판에 `⏹ {엔딩키}`·`🎬`가 서고, 안 채운 연출이
  검증 보고에 목록으로 선다. 규격 6단계 **전부 닫힘**
- **성능 (2026-08-18)** — 첫 화면 58.3초 → 1.66초, 갱신 426 → 111ms. 고정은 시간이 아니라
  **일의 횟수**로 건다(`ChapterGraphWorkAmountTests`)
- **깃발(bool 스탯) 지정 (2026-08-19)** — 저작 쪽 완료. 계약 쪽 한 칸 대기(§7 열린 것)
- **W64 코어 사본 최신화 (2026-08-20)** — `Ked.Presentation.Core`를 런타임 정본으로 전면
  동기화(소스 35 + 순수 테스트 18 + 튜닝 실덤프 픽스처). **패키지화는 보류**, 대신 반복
  가능한 동기화 절차를 `src/Ked.Presentation.Core/README.md`에 명문화했다. 표정이 코어
  상태에서 빠져(변형만 상태, 표정은 인자) 보완 폴드 축으로 내려왔고, 새 튜닝 축
  `role-anchor`를 로더가 싣기 시작했다(안 실으면 show마다 진단 — 실구멍이었다)
- **W65 카탈로그 최신화 (2026-08-20)** — 연출 어휘 **179 → 126종**(카테고리 20 → 15).
  런타임이 연기 커맨드를 스파인으로 넘기며 사라진 **56종 폐지**(소유자 결정) · `focus_on`·
  `focus_clear` 추가 · **`face_swap`은 툴 전용 표현으로 유지**(직접 조작이 쓰는 낱말인데
  런타임 등록이 없다 — 툴이 없는 커맨드를 만들고 있었다). 도달 못 하게 된 폴드 분기 5개를
  걷었고(대사창 종류의 새 주인은 surface 프리셋), **어휘 대조를 픽스처 + 테스트로 고정**해
  다음에 갈리면 테스트가 먼저 운다
- **W66a 이동 수치 편집 (2026-08-20)** — 카탈로그 **`motion` 선언**(어느 인자가 무슨 축을
  미는가의 유일한 근거, 지금은 `move_by` 하나) · `MotionInspection`(시작은 폴드, 종점은
  런타임의 `MoveByReduction`, 단위·시간은 토큰 파서 — 사본 0) · 무대에 **칩·궤적·출발
  고스트**와 **슬라이더 조절창**. 정지 프레임은 이동이 *끝난* 자리라 출발을 그려야 연출이
  보인다. 끄는 동안은 렌더만, 손 뗄 때 한 번 저장(되돌리기 1 = 조작 1). 이징은 값만
  보이고 못 만진다 — 실을 자리가 런타임에 없다
- **W66b 실제 이징 재생 + 프레임 스크럽 (2026-08-20)** — 런타임이 ease-open-orders를
  전부 개통(코어 `Ease/` 2파일 + `ease-golden.json` + `move_by` 다섯째 인자). 반입 후
  재생 보간의 선형 `Lerp`를 **코어 `EaseFunctions`**(DOTween과 저쪽 비트 동일, 이쪽
  골든 픽스처 35종×257샘플 <1e-4로 이중 고정)로 교체 — move_by가 진짜 OutCubic으로
  논다. 칩 아래 **프레임 스크럽**이 라인 배치의 내부 시간을 끈다(재생과 같은 보간·같은
  곡선에 진행도만 흘린다). 남은 것: W67(카탈로그 ease 칸 + 선택기 — 런타임 인자는
  이미 열려 있다)
- **W67 + 커스텀 곡선 (2026-08-20)** — ease 다섯째 칸(기본값 없음 → 미지정 대본 불변) ·
  이징 선택기(곡선 미리보기 = 재생과 같은 함수) · **마야식 곡선 그래프 에디터**(선택
  이징을 5키로 베이크해 출발 → 키·탄젠트 편집 → 프로젝트 저장 + 커맨드 `@이름`).
  곡선은 프로젝트 소유(작가 자산), `curves.json`(배열+name, 런타임 확정 스키마)은
  내보내기 산출물, 없는 `@이름`은 내보내기 차단. 프리뷰·재생·스크럽·유니티가 코어
  `CurveFunctions` 하나를 쓴다. **곁가지 수리**: undo 스냅샷 코덱이 writerSpeakers를
  안 실어 작가 화자가 undo에 사라지던 잠복 버그 — 직렬화 헬퍼 공유로 수리
- **프리뷰 탭 정리 — 대본 터미널 + 작업대 (2026-08-20~21, 다듬기 1~3차)** — 무대
  프리뷰가 셋째 탭으로 승격(분리 창·접기 삭제)하고 좌측에 **대본 터미널**(시나리오
  전체 텍스트 · 선택 라인 반투명 박스 · Rider식 점 = 상세조절 · 좌클릭 한 번 = 커맨드
  선택 · 드래그 = 자리 이동 · 행 우측 X = 제거 · 라인 박스 위 lineId 헤더)이 섰다.
  그 아래 **작업대**가 우측 편집기의 그 행 빌더(체크·칩·갤러리·직접 입력)로 선택
  라인 또는 Setup(구획 클릭)을 편집한다 — 우측 열에는 노드 수준만 남았다. 자리
  이동·제거는 터미널, 값 편집은 작업대로 역할이 갈라졌고(작업대 ▲▼·✕ 삭제),
  터미널 선택이 작업대 띠와 연동된다. 터미널:작업대 영역비는 가로 스플리터 드래그
  (작업대는 고정 높이 + 내부 스크롤). 통로: `PresentationScriptPanel`(그리기와
  신호뿐) · `MovePresentationCommand`(이동 하나 = undo 하나). **4차**: 라인마다
  `<<actor @2 willow>>` 액터 선언 행(캐스팅 폴드에서 배역, 커맨드 표시의 무대 대상도
  별칭 치환 — 저장 인자는 슬롯 그대로) · X는 선택 구획에서만 · 터미널 툴팁 전무 ·
  조절 콘솔 [이동] 탭 삭제("이동도 커맨드 하나") · 스크롤바가 X를 가리지 않게 우측
  여백. **5차**: **작업대 = Inspector**(`BuildCommandInspector` — 선택 커맨드 하나의
  편집 행 + 무대 수치 조절을 한 판으로, 선택 없으면 [＋연출 추가]만; 라인 커맨드 전체
  나열·띠 연동은 대체 삭제) · **점(●) = 켜고 끄기 토글**(꺼진 커맨드도 흐린 행으로
  남는다 — 터미널 행이 발행 초안이 아니라 작업 중 커맨드로 지어진다,
  `PresentationScriptRow.IsEnabled`) · **라인만 재생**(`StagePlayback.PlayCurrentLine`,
  [▶ 라인] 버튼 — 다음 라인 문턱에서 멈춤) · **재생 컨트롤 + 프레임 타임라인이 무대
  아래 재생 줄로**(`BuildTimelineScrubber`, 무대 위 오버레이 스크럽·상세조절
  플라이아웃 삭제). **재생 줄 = `[▶ 라인] … 타임라인 … [◀ 이전][다음 ▶] … 1/3 [전체] [1×]`**
  — 라인 넘기기는 2026-08-22에 붙었다(소유자: 고르기·속도·재생이 한 줄에 서야 손이 판을
  안 가로지른다). 이동은 자동 진행과 **같은 통로**(`MoveBy` → `MoveRequested` →
  활성 편집기)이고, 끝에서는 회색이며, **재생 중에 옮기면 재생이 멈춘다**(자동 진행이
  기다리는 이동과 사람의 이동이 같은 자리를 다투지 않게).
  ⚠ **타임라인은 프레임 격자다** (2026-08-22 소유자): 눈금 한 칸 =
  한 프레임(`TickPlacement`가 없으면 `TickFrequency`가 있어도 **안 그려진다**) ·
  손잡이가 프레임 사이에 못 선다(`ValueChanged`에서 반올림 — 스냅 설정만으로는 끄는
  동안 미끄러진다) · **재생이 손잡이를 민다**(`SyncTimelineHandle`). ⚠⚠ 되먹임 빗장
  (`_syncingTimeline`)이 필수다 — 없으면 손잡이가 무대에 진행도를 되받아 쳐서 재생의
  보간이 프레임 격자로 덮이고 두 값이 서로를 밀며 떤다. **6차 — 클릭이 크기를 바꾸지 않는다**: 선택은 하나(Setup↔라인
  배타) · 제거 X는 자리를 늘 잡고 보이기만 바뀐다 · 작업대는 비어도 접지 않는다
  (높이를 바꾸는 것은 사람의 스플리터 드래그뿐) · 뱃지 28 · 알림 46 · 재생 줄 34로
  높이 고정하고 넘치면 각자 안에서 스크롤 — 경고가 떠도 무대가 밀리지 않는다.
  **7차 (2026-08-21)**: 작업대의 상시 [＋연출 추가] 줄 제거 — **추가는 터미널 우클릭
  메뉴**([＋ 연출 추가]가 **연출 추가 콘솔**을 플라이아웃으로 띄운다: 검색(전 종류 평면
  결과) · **종류별 탭**(최근 · ★프리셋 · 카테고리) · 직접 입력. 최근이 비었으면 첫
  카테고리 탭으로 열려 빈 화면이 안 뜬다 — `PresentationNodeEditor.BuildAddConsole`).
  우클릭 메뉴에
  **복사·복제·붙여넣기**가 서고 **Ctrl+C/V/D** 단축키가 같다(포커스가 터미널에 있을
  때만). 복사는 결과 행의 해석된 값이라 프리셋 없이 자립하고 노드를 오가며 붙는다.
  복제는 `ProjectEditor.DuplicatePresentationCommand`(원본 바로 뒤, undo 한 번)
  **8차 (2026-08-21)**: **타임라인 상시 표기**(끌 시간이 없는 라인에서는 비활성으로
  서고 사라지지 않는다 — 있다 없다 하면 그 도구가 있다는 사실 자체가 안 보인다) ·
  **이동의 노란 고스트 윤곽·점선 궤적 제거**(소유자: "잘 안쓰게 되네" — 어디로 가는지는
  타임라인을 끌거나 재생하면 보인다). 휴지 배치(정지 화면 = 라인 시작 순간)와 숨김
  슬롯의 회색 점선 네모는 그대로다.
- **재생 줄 정리 (2026-08-21)** — **[⏮ 처음부터] 삭제**(첫 라인은 터미널에서 고른다) ·
  노드 전체 재생의 뜻이 **"현재 라인부터 끝까지"**가 됐다(마지막 라인에서 첫 라인으로
  되감던 규칙은 ⏮과 한 쌍이라 함께 걷혔다) · **자리로 무게를 가른다**(흐리게가 아니라):
  타임라인을 사이에 두고 **왼쪽 = [▶ 라인] 하나**(왼쪽 벽에서 10px 띄움), **오른쪽 끝 =
  진행 표시 · [⟶ 전체] · 배속** (`BuildLeading`/`BuildTrailing`, XAML 컬럼
  `Auto,16,*,16,Auto`). 배속은 실제 게임에도 있는 기능이라 남는다 · **두 버튼이 각자 제
  모드만 비추고 아이콘도 갈린다**(`IsPlayingAll`·`IsPlayingLine`) — 라인은 글리프 ▶,
  전체는 **꼬리 긴 화살표 도형**(글리프는 폰트에 따라 두부가 되고 꼬리 길이를 못 정한다.
  멈춤도 같은 폭 18의 도형이라 상태가 바뀌어도 단추가 안 흔들린다). 라인만 재생 중에
  전체 버튼이 ⏸로 바뀌던 헷갈림이 정체였다(상태 하나로 라벨 둘을 정했다). 서로 누르면
  멈춤이 아니라 모드 전환 · 타임라인의 `fr` 숫자는 노랑을 걷고 평범한 색이다
- **이징 곡선 끝점 고정 (2026-08-21)** — 곡선 에디터의 첫/끝 키가 (0,0)·(1,1)에
  **자리째 잠긴다**(기울기만 편집). overshoot은 중간에서만 — 끝값이 1을 벗어나면
  리듀서가 도착을 넘어간 채로 끝나기 때문. 값까지 끌던 시절의 어긋난 끝값은 로드가
  계약 자리로 되돌린다(탄젠트 보존)
- **시간 흐름 일반화 (2026-08-21)** — `StageMotionPlan`(순수): 커맨드마다 코어 폴드의
  **직전·직후 상태 차이**를 노드 단위로 재어 구간을 만들고, 진행도마다 보간한 상태를
  **컴포저(RectChain)에 그대로** 넘긴다. 축 선언이 필요 없어 `place`·`size`(뎁스)·
  `scale_by`·`rotate_by`가 `move_by`와 같은 규칙 하나로 흐르고, **셋을 같이 써도** 각
  커맨드가 제 duration·이징으로 제 노드만 끈 뒤 컴포저가 합친다(같은 노드 겹침은 뒤가
  이긴다 = DOKill). 시간을 가졌다는 근거는 **`duration` 파라미터 선언**이고 0이면 스냅이다.
  정지·스크럽·재생이 같은 합성을 지나므로 자리뿐 아니라 **크기**도 보간된다(뎁스가
  스냅하던 원인). 이징은 **선언이 말한다** — 2026-08-21 런타임이 place·size·회전에도
  이징 인자를 열어 가정이 사라졌다
- **카메라도 시간을 탄다 (2026-08-21)** — 샷 5종(`shot_to`·`shot_focus_to`·`shot_zoom`·
  `shot_track`·`shot_reset`)이 `duration`·`ease`를 탄다. ⚠ 막혀 있던 이유는 **샷이
  노드가 아니라 `StageState.Shot`(zoom·pan·focus)** 이라 노드 차이만 보던 계획이 못 봤던
  것이다(`MotionShotTween`). 보간은 런타임 `PresentationShotIntentMath.Interpolate`와
  같은 셋 Lerp이고, **카메라가 흐르면 무대 전체 슬롯이 흐르는 것으로 센다**. 이징은
  런타임이 같은 날 브리지를 열어(항수 +1) place·size와 같은 마지막 위치 인자가 됐다 —
  미지정이면 토큰이 안 나가고 `@이름` 커스텀 곡선도 같은 자리다
- **이징 문법이 하나로 모였다 (2026-08-21)** — `move_by` · `place` 13종 · `size` 6종 ·
  `rotate` 3종 · `shot` 5종 · `scale` 3종이 **마지막 위치 인자 = 이징**이라는 같은 규약을
  쓴다(미지정이면 토큰 생략 · `@이름`이면 커스텀 곡선). 툴은 파라미터 `type`만 보므로
  카탈로그에 칸을 더하는 것으로 끝난다 — 계획·검증 코드는 안 건드린다.
  **몸짓 커맨드 `gesture` (2026-08-21 개통)** — 최종값 무변 + 축별 곡선, 표적은
  `CharacterPortrait_Shake`(이동 계열과 다른 노드라 같은 라인에 겹쳐 "총총 뛰며 이동").
  ⚠ **폴드가 무변**이라 계획이 상태 차이가 아니라 **인자**로 읽는다(`MotionGestureTween`) ·
  진동은 이징을 안 탄다(런타임도 Linear로 흘리고 곡선이 모양을 진다) · 곡선이 **두 종류**로
  갈렸다(이동 (1,1) · 진동 (1,0), 판정은 코어 `CurveKindRules` 하나를 툴·런타임이 공유 —
  종류가 어긋나면 내보내기가 막는다). 규격: [`../work-orders/gesture-orders.md`](../work-orders/gesture-orders.md)
  ⚠ **인자 자리 밀림 결함**이 여기서 드러나 함께 고쳤다 — 중간이 빈 선택 인자는 자리표
  `""`로 메운다(예전에는 끊어서 뒤 인자가 앞 자리로 밀렸다).
  **핑퐁 (2026-08-21 증보)** — 곡선 자리가 **표준 이징 이름**도 받는다. `PingPong(ease, t)`
  = 왕복의 절반이라 끝값 0이 수식으로 지켜지고, 35종이 그대로 몸짓이 된다(OutBounce 통통 ·
  OutElastic 떨림 · Linear 각진 jolt). 준비 과정 없이 낱말 하나로 쓴다. 우선순위(곡선 →
  이징 핑퐁 → 기본 혹)는 코어 `OscillationSource` + `OscillationFunctions.Evaluate` 하나가
  진다. ⚠ **기본 혹 = `OutSine` 핑퐁과 같은 함수**(오차 0) — 기본이 선택지 하나의 별칭이라
  선택기에서 OutSine을 고르고 [곡선 편집…]하면 기본 혹이 키로 펼쳐진다.
  **작업대 (2026-08-21)** — `oscillation` 칸이 이동과 같은 선택기로 서되 미리보기를 핑퐁으로
  그리고, [곡선 편집…]은 그 핑퐁을 **9키로** 굽는다(가운데가 봉우리라 5키로는 못 잡는다).
  ⚠ 콤보의 빈 칸 기본은 **종류마다 다르다** — 이동 `OutCubic` · 진동 `OutSine`(기본 혹과
  같은 함수라 화면과 재생이 일치하고 인자도 안 늘어난다).
  `char_scale_to`는 2026-08-21에 **코어로 이관**했다(`ApplyPortraitScaleTo` — 표적은
  초상 축 `CharacterPortrait_ActingScale`이라 `scale_by`의 `CharSlot_Scale`과 겹쳐도
  서로를 안 덮는다. 양쪽 저장소 동일 · 유니티 검증은 소유자 몫).
  `focus_on`·`char_visual_*`은 런타임 이징 미개통이고, `mirror`는 안 하기로 했다
- **등속 이동 4종 폐지 (2026-08-21)** — `left_per` 계열이 사라졌다. 속도가 **1u/프레임에
  잠겨** 있어 "프레임당 특정 거리"를 못 냈고, 같은 노드에 같은 클레임을 거는 넛지가 그
  일을 다 한다(`left c1 12u 12fr` ≡ `left_per c1 12fr`). 실사용 0건 · 소유자 확정.
  카탈로그 129 → **125**. 코어 사본·어휘 픽스처도 런타임과 맞췄다
- **넛지 거리 슬라이더 (2026-08-21)** — 작업대에 **`unit` 타입 갈래** 신설. 값이 `3u`라는
  토큰이라 숫자 슬라이더로는 읽지도 쓰지도 못한다(`UnitToken`을 지나고 숫자 갈래보다
  먼저 본다). 부호는 안 붙인다 — 방향은 커맨드 이름이 진다. 카탈로그 넷에
  `slider {0, 6, 0.25}`. 넛지 이징도 런타임이 열어(항수 3→4) 같은 날 붙었다 — 이로써
  이징 문법이 `move_by`·place·size·rotate·shot·scale·넛지에서 하나다.
  **샷 x·y도 슬라이더**(`shot_to`·`shot_track`, `-12~12`) — `signedUnit`은 부호를 붙이고
  `unit`은 안 붙인다. ⚠ 슬라이더는 **카탈로그 선언이 있을 때만** 선다("선언이 정한다")
- **이징 개통 + 회전 복원 (2026-08-21)** — 런타임이 `place`·`size` 계열 20종에 이징
  인자를 열고(마지막 위치 인자 · 미지정 OutCubic · `@이름` 커스텀 곡선) 회전 3종
  (`rotate_by`·`rotate_reset`·`char_rotate_to`)을 되살렸다. 이쪽은 카탈로그 반영
  (126 → **129**), 코어 사본 동기화(`ApplyPortraitRotateTo` + 라우팅), 어휘 픽스처
  재스캔(저쪽 등록과 정확히 일치), **이징 선택기 일반화**(타입이 `ease`인 파라미터면
  무엇이든 곡선 미리보기 + 커스텀 곡선 편집 — place·size·회전이 이동과 같은 편집이다)
- **라인 시계 상한 제거 (2026-08-21)** — `StageTransitions`의 1.0초 상한이 정확히
  24프레임이라 그보다 긴 커맨드가 중간에 잘리고 스냅했다. 상·하한을 걷어 **커맨드가
  쓴 프레임이 곧 라인이 흐르는 시간**이 됐다(시간 가진 커맨드가 없을 때만 기본 0.35초).
  작업대 시간 슬라이더 상한도 적힌 값에 맞춰 늘어난다
- **detour 전환 + 커스텀 노드 출구 폐지 + 연출 채널 자동화 (2026-08-21)** — 조건
  갈래의 커스텀 씬이 `<<jump>>` → `<<detour>>`(갔다가 돌아온다, 계약 A2-1) · 커스텀
  노드의 출구 폐지(복귀가 곧 출구, A2-2, `EffectiveDefaultExit` 한 곳) · **발행·배선
  자동화**: 연출 그래프의 다섯 걸음(대사 발행 → 연출 노드 → 결과 연결 → 연출 발행 →
  공급 연결)이 무대 프리뷰 **씬 선택기 한 번**으로 줄었다
  (`ProjectEditor.EnsurePresentationChannel` — 멱등, 내용 같으면 버전 불변, binding
  불가침). 연출 노드는 배관으로 숨고 데이터·내보내기 짝은 그대로다 · 뒷정리로
  **[편집 자료] 현황판 삭제**(에셋 탐색기는 남는다)와 **연출 공급을 판에서 제거**
  (단추·필터 체크란·카드·간선 · 편집기 뷰 삭제 · 범위 해석은 불변)
- **무대 조절창 [★ 자주 쓰는] 탭 (2026-08-22)** — 무대를 클릭해 열리는 조절창의 **맨 앞
  탭이자 기본 탭**. 소유자: *"비개발자가 보기에 직접 연출을 추가하는 건 부담이 커 …
  엄청 자주 쓰이고 유용한 것들을 … 핫키 느낌으로."* 네 탭이 덮는 것은 "누가·어디·어떤
  표정"이고 **카메라만 어느 탭에도 없었다** — 그 바깥은 여태 터미널 우클릭의 **126종 ×
  15탭**으로만 닿았다. **기본 칩은 샷 셋뿐**(`줌 인`·`카메라 이동`·`카메라 원위치` —
  같은 날 소유자), 나머지는 사람이 담는다.
  - **담기 = 탭 안 [편집] → 터미널의 커맨드 행 클릭**(`PresentationScriptPanel.PinMode`).
    ⚠ **행마다 글리프를 갈지 않는다**(소유자: "X를 ★로 바꾸는 건 최악의 아이디어야") —
    글리프가 달라지면 글리프 높이가 달라지고 그것이 곧 줄 높이라 터미널이 꿈틀거렸다.
    대신 **판 전체가 활성으로 보인다**(테두리 색 + 바탕 + 행 띠 + 손 커서). 테두리는
    **평소에도 2px 자리를 잡고 있어** 켜고 꺼도 한 픽셀도 안 움직인다
  - ⚠ **조절창은 팝업이 아니라 무대 오른쪽 붙박이 기둥이다** (2026-08-22 소유자:
    "연출 프리뷰 오른쪽에 상시 표시 — 챕터그래프와 연출그래프에서처럼"). 판의 열은
    `380,10,*,6,340`(터미널·무대·조절창)이고, 내용은 팝업 시절과 **같은 함수**
    (`BuildStagePopover`)가 짓는다(`BuildDockedConsole`이 담는다).
    **딸려 죽은 것**: 끌기 손잡이·✕·우클릭 닫기·라이트 디스미스·"무대 빈 곳 좌클릭 =
    조절창 열기" — 닫히지 않는 판에는 닫는 손짓이 없다. 초상 클릭은 남되 **여는 것이
    아니라 가리키는 것**이다(그 슬롯의 [캐릭터] 탭).
    ⚠⚠ **`Render()`가 끝날 때 판을 다시 그린다** — 슬롯·표정·수치가 전부 이 라인의
    것이라 라인이 바뀌었는데 옛 판이 서 있으면 거짓말이다(팝업 시절엔 다시 열 때 새로
    지어져 없던 의무다). **잠긴 화면에서는 탭 대신 이유 한 줄**이 선다.
    ⚠⚠ **값 시뮬 팝업만 팝업으로 남았다** — `ShowManipulationFlyout`은 이제 그것만
    섬기므로 `_consoleRebuild`·담기 모드를 건드리지 않는다.
    ⚠⚠⚠ **팝업의 입력 동작은 헤드리스가 증명해 주지 못한다** — 헤드리스는 창 안
    오버레이(`TopLevel.GetTopLevel(child)`이 `Window`)지만 실기 Windows는 별도 네이티브
    팝업 창이라 입력 경로가 다르다. 고정 테스트가 초록인 채로 실기가 깨진 실사례가 있다
    (우클릭 닫기를 두 번 고치고도 실기에서 안 닫혔다 — 결국 판이 붙박이가 되며 끝났다)
  - ⚠ **대사 노드로 이 탭에 들어오는 것 = 씬 선택** (`MainWindow.EnterStageTab`).
    대사 노드가 선택돼 있으면 프리뷰가 **공급된 발행 결과**를 그리고 발행본은 불변이라
    직접 조작이 잠긴다 — 잠긴 화면에서는 무대를 눌러도 조절창이 안 열려 막다른 길이었다.
    이제 씬 선택기와 같은 함수(`EnterPresentationChannel`)를 지난다(멱등)
  - ⚠ **인자를 통째로 담는다 — 슬롯도 duration도 그대로**(소유자: "그대로 복사하는게
    포인트"). 선택 슬롯이 들어가는 것은 **칩이 그 자리를 안 담았을 때뿐**이고, 그것도
    없으면 그 칩만 회색이다. 대가: 슬롯 이름이 다른 씬에서는 없는 슬롯을 가리킨다
    (막지 않는다 — "반영 안 됨"이 짚는다)
  - **[편집]의 칩은 [▸][이름 칸][✕]** — ▸를 펴면 **작업대와 같은 슬라이더·선택기**로
    수치를 만진다. ⚠ 위젯은 한 벌이고 쓰는 곳만 다르다(`ArgumentSink` — 노드 커맨드는
    `CommandSink`, 칩은 `SetQuickCommandArgument`). 칩에서 **대상 교체와 커스텀 곡선
    편집은 안 된다**(전자는 작업대 규칙 그대로, 후자는 곡선이 노드 커맨드의 자산이라서)
  - 이름은 안 묻는다(정의 이름 + 충돌 시 번호). **Enter와 초점 잃음 둘 다** 커밋한다 —
    후자가 없으면 이름을 치다 터미널을 클릭한 순간 날아간다
  - 재클릭은 값만 바뀐다 — `PresentationStageActions.Apply` 규칙을 칩도 그대로 탄다
  - ⚠ **`QuickCommands`의 `null`(손대지 않음)과 빈 목록은 다르다** — 전자는 manifest에
    키가 없고 코드 기본이 서고, 후자는 빈 채로 남는다. `screen_flash`·비네트는
    `presetKey` 어휘를 몰라 뺐다(열린 항목)
- **흔들리던 테스트 (2026-08-19)** — 어서션이 아니라 **프로세스가 죽고 있었다.** 닫지 않은
  파일 감시자가 타이머 스레드에서 없어진 디스패처에 `Post`를 했다. `DetachSession()` ·
  `OpenChapterViews`(테스트가 띄운 화면을 폴더 삭제 전에 닫는다) · `Fire()`의 두 번째 방벽

### 열려 있는 것

**저쪽 대기 (한 칸씩)**

| 항목 | 상태 |
|---|---|
| **⛔ 깃발을 실을 칸** | `StatChangeDto`에 `Op`("Add"/"Set")가 없어 **깃발을 쓰는 챕터는 내보내기가 거부된다.** 없으면 `"Add"`라 기존 파일은 안 깨진다. 서는 날 `BoolSetNotCarried` 한 함수만 지운다. 규격 [`../work-orders/bool-stat-orders.md`](../work-orders/bool-stat-orders.md) — **소유자가 전달해야 한다** |
| **증명기 오라클 통보** | `ChapterReachabilityProver`는 이제 저쪽의 **고정된 오라클**이다(코퍼스로 등가성 고정). 깃발 지정으로 `ApplyDeltas`가 바뀌었으므로 알려야 한다 — 다만 **코퍼스 일곱 케이스에 지정이 없어 기존 답은 안 바뀐다.** 위 규격 §3에 함께 들어 있다 |
| ~~place·size의 tween 이징 대조~~ | **닫힘 (2026-08-21)** — 가정(OutCubic)이 맞았고, 나아가 런타임이 **이징 인자를 실제로 열었다**(place·size 마지막 위치 인자, `@이름` 커스텀 곡선 포함). 카탈로그 20종에 이징 칸을 넣었다 |
| ~~뎁스 레벨 커브 폴드~~ | **닫힘 (2026-08-21)** — 저쪽이 프리셋 표를 버리고 라벨을 레벨 커브 위의 눈금으로 통일했다(`DepthLevelLabels`). 이쪽은 사본 동기화 + 로더 한 줄 + 작업대 입력 합치기로 반영. 규격: [`../work-orders/depth-level-orders.md`](../work-orders/depth-level-orders.md) ⚠ **라벨 재생값이 바뀌었다**(mid의 y 0 → -160 등) — 에디터 육안 확인 뒤 눈금을 옮기고 싶으면 `DepthLevelLabels` 상수 하나다. 커브 설계 구간은 2026-08-21에 **[0,20]으로 확장**(배율 상한 2.2, close에 무릎을 두어 라벨 다섯의 값은 불변) — 작업대 슬라이더는 -10~20. 커브 값 일곱 점은 `RuntimeTuningLibraryTests`가 고정한다(덤프가 낡으면 먼저 운다) |
| **회전 복원 — 이쪽 반영 끝, 저쪽 육안 확인 대기** | 런타임이 `rotate_by`·`rotate_reset`·`char_rotate_to`를 되살렸고(2026-08-21) 이쪽은 카탈로그 3종 복원 + 코어 사본 동기화(`ApplyPortraitRotateTo`) + 픽스처 재스캔까지 끝. ⚠ `char_rotate_to`는 구판의 다단 연출(PivotRotateTo)이 아니라 **절대 회전 한 겹**이다 — 다단이 필요하면 저쪽에 요청. "portrait 넣는 커맨드"는 여전히 무엇인지 확인 대기 |
| **ease 개통 — 저쪽 Unity 에디터 확인 (1·2차 묶음)** | 1차(이징)·2차(커스텀 곡선) 개통 모두 **2026-08-20 완료, 산출물 반입 끝**. 저쪽에 남은 것은 에디터 확인 한 묶음: 리컴파일 · EditMode 러너(DOTween 등가 + AnimationCurve 등가) · 골든 덤프 메뉴 1클릭 · 코퍼스 스모크 · `12fr Linear`·`@hop_snappy` 육안. 스탠드얼론 대조는 둘 다 통과(이징 오차 0 · 커브 3e-7) |

**소유자 판단 대기**

| 항목 | 상태 |
|---|---|
| **깃발의 수명** | 챕터를 넘으면 리셋되나? 저쪽 D1(스탯 수명)과 한 덩어리다. 깃발이 방금 생겨 바로 따라오는 질문 |
| **세이브가 돌아올 것인가** | 답이 바뀌었다 — **저쪽이 G4(세이브)를 직접 세웠다.** 돌아오면 옛 §C1(`#line:` 필수)·§C3(선택지 순서 동결)이 그대로 되살아난다 |
| **`EndingRules`를 언제 채우나** | 모양은 저쪽에 이미 있다(`EndingRule`·`ScenarioProgression`·`ScenarioTransition`, 픽스처로 돈다). 이쪽이 빈 배열로 낼 뿐 — **"챕터를 잇는 판을 만들 것인가"**와 같은 질문이다 |
| **`Ked.Presentation.Core` 패키지화** | **소유자 결정(2026-08-20): 패키지화는 시간이 없어 보류, 대신 사본 최신화.** W64로 동기화 완료 — 런타임이 정본, 절차는 `src/Ked.Presentation.Core/README.md`. 다음 동기화가 필요해지는 날(예: EaseFunctions가 저쪽에 서는 날) 같은 절차를 탄다 |
| **부착 표시조건** | v8에서 관문이 간선으로 내려가며 갈 곳을 잃었다(들어오는 간선이 없다). 부착을 실제로 쓸 때 터진다 |
| **에셋 탐색기 접근** | 2026-08-22에 탐색기가 연출 그래프 안으로 들어가며 **거기서만 닿는다**. 잃은 것 둘: **탐색기 → 무대 드래그**(둘이 동시에 안 보인다 — 대체는 조절창의 [배경]·[캐릭터] 탭) · **[새로 고침]**(에셋·튜닝 재읽기)이 챕터 그래프·무대 프리뷰에서 안 닿는다. 되살리려면 탐색기만 창으로 되돌리거나 무대 프리뷰에 한 벌 더 둔다 — 소유자 판단 |
| **에셋 새로 고침이 UI 스레드를 잡는다** | png 재귀 스캔 + 튜닝 재읽기 + 트리 재구성 + 비트맵 재디코드가 전부 동기다 — 큰 폴더에서 30초 넘게 창이 굳어 **멈춘 줄 알고 죽이면 저장 안 한 편집이 날아간다**(2026-08-21 소유자 보고). 지금은 단추 잠금 + "다시 읽는 중" 표시로 **읽히게만** 해 뒀다. 근본 해결은 스캔을 백그라운드 스레드로 옮기는 것인데 `RefreshAssets` 호출처가 일곱 군데다 |
| **⚠ Tier 2 영속화** | `EpisodeSelectionStateData`가 어디에도 저장되지 않는다 |
| **T3 재시도 여부** | 소유자가 "어느 부분이 어색했는지" 아직 답하지 않음. 코드는 `5841eac`에 보존 |
| **연출 커맨드 재정비 진행 중 (2026-08-20 재조정)** | **W64·W65 닫힘**(아래 §7 닫힌 것). 남은 순서: MoveBy 슬라이더 편집(W66) → ease(W67) → 계통 확장(W68+ — Move·Anchor·Place·ShotResponse·Depth). 계획 [`../work-orders/presentation-refresh-orders.md`](../work-orders/presentation-refresh-orders.md). 곡선 드래그 에디터([`motion-graph-orders.md`](../work-orders/motion-graph-orders.md) MG1–4)는 **보류** — 슬라이더가 아트 검증을 통과한 뒤 |
| **폐지 커맨드가 남은 옛 프로젝트의 발행** | W65로 56종이 카탈로그에서 빠졌다. 그 커맨드가 남은 노드는 발행 텍스트가 `<<char_rig_acting.hop c1>>`처럼 **정의 Id 그대로** 나가 런타임이 거부한다. 프리뷰 뱃지는 "반영 안 됨"으로 짚지만 발행을 막지 않는다 — **차단이냐 경고냐**가 판단 대기(연출 쪽엔 검증기가 없어 차단하려면 기반부터 지어야 한다). 실데이터에는 연출 커맨드가 0이라 급하지 않다 |
| **조건 삭제 기능** | 소유자가 **보류** 결정 (기록만) |
| **`presetKey` 어휘** | `screen_flash`·`screen_vignette`·`char_visual`의 프리셋 이름을 툴이 모른다(`ArgumentTokenCandidates`에 후보 없음). 그래서 [★ 자주 쓰는] 기본 칩에서 플래시·비네트를 뺐다 — 한 번 눌러 유효한 커맨드가 안 나온다. 런타임 저장소에서 키를 확인하면 후보 표에 한 줄, 칩 두 개다 |

**정리 대상 (급하지 않음)**

| 항목 | 상태 |
|---|---|
| **연출 그래프의 v6 잔향** | 철도 칩·가지 순서가 아직 간선의 파생 `OptionLabel`로 돈다(동작함). 칩 순서를 선택지 시트 인덱스로 옮기는 정리는 미착수 |
| 합류선 밀집 | 웹이 커지면 레인 끝에 합류선이 몰릴 수 있다. 계단식 분산 또는 웹 접기를 검토 |
| **미검증 — 관통 시연** | 저작·계약 양쪽이 다 섰지만 **실제 게임에서 한 챕터가 끝까지 도는 것을 아무도 안 봤다.** 저쪽 유니티 채택에 `.meta`가 필요하고 그건 저쪽 저장소 밖 일이다 |

---

## 8. 여기까지 온 길 (전부 커밋됨 — run-log 상세)

한 날에 규격이 세 단계로 굴렀다. **순서가 곧 근거다**:

1. **화자 시트** (`5f77b30` 커밋됨) — 챕터 `화자` 시트 → 에피소드 워크북 화자 드롭다운
2. **v5 규격** — 간선 열 재배치·인덱스 폐지·조건 구조화(스탯/연산자/값)·스탯 타입·`<` `>`·픽스처 제거·자동 이행기
3. **편집 UI 개편** — "엑셀에서만 편집" 체크(기본 켬, 툴 읽기 전용) · 챕터 클릭 = 엑셀 열기 · 단추 없는 저장 · 탭 유지 · [＋] 연결 폼
4. **선택지 v6 → v7 → v8** — 선택지 정본이 챕터 `선택지` 시트로. v7에서 칸 수를 에피소드가 정하고, v8에서 관문이 간선으로 옮겨 갔다
5. **엑셀 잠금 가시화** (`ac3b9d5`) — "추가가 안 된다"의 정체는 엑셀이 챕터 파일을 열고 있어서였다. 붉은 배너 + 원인을 이름으로 부르는 사유
6. **v9 — 선택지 시트가 전역 문구 사전으로** (2026-08-17): 칸이라는 개념 폐지. 간선 D열에 **문구 그 자체**가 들어가고, 사전은 출발 없이 인덱스·대본뿐이다. 어느 에피소드에서든 모든 문구를 고른다

그 이전 커밋 흐름(2026-08-15): `79466df` 인수인계 · `8836124` 한 구간에 선 하나 ·
`f82beaf` A계층 격리 · `6f8940b` 낙하 폐지(→ v6에서 방어장치로 개정) 등 — run-log 참조.
