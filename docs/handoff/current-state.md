# VnTool 현재 상태 — 이어받는 세션을 위한 인수인계

기준: 2026-08-16 · 테스트 **1062 통과, 실패 0**
(Ked.Presentation.Core 220 · Vn.Core 60 · Vn.Authoring 612 · Vn.App 170)

> **⚠ 2026-08-16 하루에 규격이 v5 → v6 → v7 → v8로 굴렀다. 이 박스가 최신 계약이다.**
>
> **시트 규격 (v8)** — 챕터 워크북 7시트:
> - `에피소드`: EpisodeId·제목·종류·대사엔트리·X·Y·엔딩키·메모·**선택지수(I)** (+선택 `도달불가 허용`). **인덱스·표시조건·해금조건 열 폐지**. 선택지수 = 그 에피소드 끝 Choice의 Option 칸 수(기본 1) — **칸 수를 정하는 것은 에피소드다**
> - `간선`: 출발·도착·**스탯변화(C)**·**선택지(D = 짝 칸의 인덱스)**·**표시조건(E)**·**해금조건(F)**·잠금시 숨김·잠금 안내문. **간선 하나 = 선택지 칸 하나(1:1)**, 신원 = (출발, 인덱스). **관문은 전부 길의 것**(v8 — "보일지 말지는 간선이 정한다"): 표시조건이 서야 보이고, 해금조건이 서야 고를 수 있다
> - **`선택지`(신설)**: 출발(근원 에피소드)·**인덱스**·**대본**·메모 — **도착 없음**(도착은 간선 소유). 인덱스가 칸의 신원이고, 칸이 갖는 것은 순서와 **자유 수정 대본 text**뿐. **빈 text = 보이지 않는 기본**(에피소드당 1, 조건 없는 간선과 짝 — 어떤 조건도 안 될 때 빠지는 방어장치). 간선의 `OptionLabel`은 짝 칸 text의 **파생값**
> - 칸은 미리, 간선은 이을 때: 선택지수를 올리면 칸만 서고 도착을 고르는 순간 간선이 된다. **안 이은 칸은 정상**(그 길을 고르면 챕터가 거기서 끝난다)
> - `조건`: 라벨·**스탯(드롭다운)·연산자(드롭다운)·값**·설명 — 연산자·값을 비우면 스탯 칸 전체가 원문 조건식(cleared:·복합식 탈출구). bool 조건은 연산자 칸이 true/false, 값 칸 회색
> - `스탯`: 스탯키·표시명·초기값·최소·최대·**타입(int/bool)**. bool은 경계 0·1 고정, 크기 비교·증감 금지(오류)
> - `화자`: 이름·캐릭터키·메모 — 에피소드 워크북 H열 드롭다운의 재료(원고 파일의 숨김 `화자목록` 시트를 갈아 끼움 — v4의 유일한 예외, 대사 행 불가침)
> - ~~`픽스처`~~: 임시 제거(있으면 읽음, 새로 안 만듦, UI 숨김)
>
> **전이 규칙 (v8)**: ① 고른 선택지 칸의 짝 간선(표시·해금조건이 서야 뜬다 → StatChanges 커밋) ② 못 고르면 **보이지 않는 기본** 칸의 간선 ③ 없으면 챕터 종료. 관문·잠금·스탯변화·도착은 전부 간선 소유, 선택지 칸은 순서(인덱스)와 문구뿐.
>
> **툴 편집 탭 = 선택지 목록**: 줄 하나가 칸 하나(`10 · 문구 → 도착`). [＋]는 칸 추가(선택지수 +1), 줄 클릭은 그 줄 아래에서 도착 잇기/바꾸기, ✕는 칸 삭제(이어진 간선도 함께). 길(간선)을 클릭하면 표시·해금조건·스탯변화를 편집한다.
>
> **⚠ v8의 알려진 손실**: 들어오는 간선이 없는 에피소드(부착)의 관문은 옮길 곳이 없어 이행에서 사라진다. 부착의 표시 제어는 별도 규격이 필요하다(열린 항목).
> **대본 엑셀의 CHOICE/OPTION은 폐지 수순** — 새 템플릿 유형 드롭다운은 IF뿐, 남아 있으면 검증이 "선택지 시트로 옮겨라" 경고.
> **연산자 `<`·`>` 개방** — ⚠ 내보내기 Op에 `GreaterThan`/`LessThan`이 새로 나가고 전이 규칙도 바뀌어 **런타임 계약(Gate D) 개정 필요**.
> **구판 파일은 앱이 열 때 `ChapterWorkbookMigrator`가 자동 이행**(.bak 남김 — 구→v5→v6→v7→v8 한 번에).
> **대본은 챕터별 폴더 `episodes/{챕터}/`에 산다** (2026-08-16) — EpisodeId는 챕터 안에서만 유일하다. 뿌리에 평평하게 있던 구판 원고는 동기화가 그 챕터 폴더로 옮긴다(같은 이름을 여러 챕터가 쓰면 옮기지 않고 경고).
>
> **편집의 기본은 엑셀이다** — 우측 위 "엑셀에서만 편집" 체크(기본 켬) = 툴 읽기 전용. 체크를 풀면 단추 없이 저장(Id는 Enter로 개명, [＋]로 칸 추가, 줄 클릭 = 그 아래 폼에서 도착 잇기). 챕터 탭 조건 편집 UI 제거.
> **화면 규칙 (2026-08-16 폴리싱)**: 왼쪽 챕터 목록은 **더블클릭**으로 엑셀을 연다(한 번 클릭 = 선택만) · 처음 보이는 탭은 **[편집]**이고 선택이 탭을 바꾸지 않는다 · **[에피소드 삭제]는 툴바 오른쪽 끝 빨간 단추**(편집이 열리고 노드가 골라졌을 때만) · **대사는 편집 탭의 선택지 아래 접이식**(별도 탭 폐지) · 선택지 폼은 **칸 드롭다운**으로 그 에피소드의 칸들을 보여 준다.
> **안 이은 칸은 정상** — 선택지가 간선보다 많아도 된다(그 길을 고르면 챕터가 거기서 끝난다). 선택지수는 칸 수의 하한.
>
> 상세·근거·되돌리는 법: run-log 2026-08-16 항목들
> (화자 시트 → v5 → UI 개편 → v6 → v7 → v8 → 챕터별 대본 격리 → 화면 폴리싱 순).

이 문서 하나로 "무엇을 왜 만들고 있고, 지금 어디까지 됐고, 다음에 무엇을 해야 하는지"가
서게 하는 것이 목적이다. 세부는 각 절이 가리키는 문서로 간다.

| 이 문서가 가리키는 곳 | 무엇이 있나 |
|---|---|
| [`../run-log.md`](../run-log.md) | **결정의 정본.** 항목마다 한 일·근거·되돌리는 법. 새 작업은 여기에 한 항목을 남긴다 |
| [`../work-orders/chapter-graph-orders.md`](../work-orders/chapter-graph-orders.md) | 규격 원본 — 엑셀 시트·열, 기능 G1–G9, 전이 규칙 |
| [`../chapter-layer-guide.md`](../chapter-layer-guide.md) | 기획자용 사용 안내 (챕터 그래프) |
| [`../episode-layer-master-plan.md`](../episode-layer-master-plan.md) | 게이트 A~D 진행 상태표 |
| [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md) | 코드 구조 정본 — "이 기능은 어느 파일" 표가 여기 있다 |

---

## 1. 무엇을 만드는가

**VnTool** — 비주얼 노벨 저작 도구. C#/.NET 10 + Avalonia, 한국어 코드베이스.
프로젝트 루트 `C:\Users\river\Documents\GitHub\java-start`, 솔루션 `VnTool.sln`.

만들어 내는 것은 유니티 런타임(별도 저장소)이 먹는 데이터다:
`exported/{챕터}.progression.json` + Yarn 번들.

### 목적 — 왜 이 구조인가

**세 사람이 서로를 막지 않고 동시에 일하게 하는 것.** 그래서 층을 갈랐다:

```
A 계층 (기획자)          B 계층 (작가)             연출 계층
챕터 그래프 · 엑셀        시나리오 그래프 · 엑셀      연출 노드
에피소드 구조             대사 한 줄 한 줄            커맨드·무대
간선 = 행선지 + 스탯변화   에피소드 안의 조건·연출     발행된 결과 위에서만
```

**각 값의 주인이 한 곳뿐이어야 한다**가 설계 전체를 관통하는 규칙이다. 같은 사실이
두 곳에 있으면 어긋나고, 어긋남은 최종 출력에서야 드러난다.

---

## 2. 계층 의미론 — 확정된 규칙 (건드리지 말 것)

### 2.1 소유권

| 값 | 주인 | 툴에서는 |
|---|---|---|
| 대사 본문·화자 | 에피소드 워크북 **`episodes/{챕터}/{Id}.xlsx`** (2026-08-16 — 챕터별 격리) | **읽기 전용.** 엑셀노드 카드는 잠긴다. EpisodeId는 챕터 안에서만 유일하므로 폴더가 챕터로 갈린다 |
| 에피소드 구조·간선 | 챕터 워크북 `chapters/{Id}.xlsx` | 툴 편집이 **엑셀 셀에 즉시 저장**된다 |
| 스탯 변화 | **간선 하나뿐** (`간선` 시트 C열 `스탯변화`) | 대본에서 스탯 조작 = 설계 미스, 폐지됨 |
| 선택지 문구·순서 | 챕터 **`선택지` 시트** (2026-08-16 v7) | 칸 수는 에피소드의 `선택지수`가 정한다. 대본 CHOICE/OPTION 폐지 수순. 조건·잠금·도착은 간선이 소유 |
| A계층 조건 식 | 챕터 `조건` 시트 | 작가 화면에는 **라벨만** 보인다 (§2.4) |
| 에피소드 안의 조건·연출 | 시나리오 그래프의 자유 노드 | 작가가 직접 만든다 |
| 줄 신원(LineId) | 프로젝트의 `ExcelLineMap` | B열은 유물. 툴은 워크북에 **쓰지 않는다** (v4) |
| 화자 등록(저작) | 챕터 `화자` 시트 (2026-08-16) | 에피소드 워크북 H열이 그 목록의 드롭다운을 받는다. **v4의 유일한 예외**: 툴이 원고 파일의 숨김 `화자목록` 시트만 갈아 끼운다(대사 행 불가침, 목록 같으면 무접촉) |
| 초상화 매핑 | `game.definition.json`의 speakers | 변함없음. 챕터 화자 시트와는 동기화가 메모리에서만 합쳐 본다 |

### 2.2 전이 규칙 (v6, 2026-08-16 — 맨 위 박스가 정본)

에피소드가 끝나면:

1. 플레이어가 고른 **선택지 칸의 주인 간선**을 탄다 → `StatChanges` 커밋(clamp) 후 이동
2. 아무 선택지도 못 고르면(조건 전부 미달·보이는 칸 없음) **보이지 않는 기본** 간선
   (빈 대본 칸과 짝인 조건 없는 간선 — 방어장치)
3. 그 외에는 **챕터 런이 여기서 끝난다**

2026-08-15의 "낙하 폐지"는 이 형태로 개정됐다 — 기본은 낙하가 아니라 **명시된 방어장치**다.

### 2.2b 관문은 길의 것이다 (v8, 2026-08-16)

표시조건(보이려면)·해금조건(고를 수 있으려면)은 **간선**이 갖는다. 에피소드 시트에는
그 두 열이 없다. 판정은 한 곳에서 끝난다 — 프루버·픽스처 워커도 간선 관문만 본다.

### 2.3 간선 신원 = (출발, 선택지 인덱스)

**간선 하나에 선택지 칸 하나**(1:1). 같은 도착으로 문구를 여럿 두려면 에피소드의
**선택지수를 올려 칸을 늘리고** 각 칸을 잇는다 — 문구는 선택지 시트의 대본 text이고
자유 수정이다(2026-08-15의 라벨 신원·OPTION 드롭다운 규칙은 이것으로 대체됨).

### 2.4 A 계층 격리 (2026-08-15 소유자 지시)

동기화가 만드는 `챕터 {판이름} 조건` 설정노드는 **시나리오 그래프에서 보이지 않는다**
(카드·공급 링크 모두 `GraphProjectionBuilder`가 거른다). A계층 식(`trust >= 3`)과
스탯 변수는 작가에게 노출되면 안 되는 자료이기 때문이다.

**단, 데이터로는 살아 있다** — 엑셀 `조건라벨` ↔ 식 역조회의 신원 저장소라 지우면
동기화가 무너진다. 작가에게는 드롭다운의 **라벨만** 남는다.

> **조건노드(SetNode) 자체는 작가의 도구다.** 에피소드 안에서만 쓰는 B계층 변수·조건은
> 작가가 직접 만들어 쓴다. 금지된 것은 "A계층 데이터를 그 자리에 부어 놓는 것"뿐이다.

### 2.5 출구 규칙

- 자유 노드의 출구 후보에 **엑셀노드는 없다** (기획의 영역으로 넘어가는 선은 금지)
- 자유 노드의 **출구 없음 = (진행)** = 에피소드 종료 후 챕터 간선이 정한다
- 엑셀노드의 옵션 포트·기본 출구는 카드가 아니라 **철도 칩**에 있다 (T2)

---

## 3. 두 화면

### 3.1 챕터 그래프 (기획자)

- 카드 = 에피소드. **선택지 포트가 카드 오른쪽**에 뚫린다 — 원천은 챕터 **`선택지` 시트의
  보이는 칸**(v6). 채워진 원 = 주인 간선 있음(클릭 = 간선 선택), 빈 원 = 주인 없는 칸(검증이
  잡음). 보이는 칸 없는 간선(보이지 않는 기본)은 카드 중앙에서 직결.
- 탭 둘: **[편집]**(처음 보이는 탭 — Id 개명·선택지 목록·[＋]/줄 클릭 폼·접이식 대사,
  길을 고르면 표시·해금조건·잠금·스탯변화) · **[챕터]**(조건·스탯 읽기 전용).
  선택이 탭을 바꾸지 않는다. 삭제는 툴바 오른쪽 끝 빨간 단추
- 아래 검증 보고 — **동기화 보고가 맨 위**(묻혀서 안 보이던 실사례)

### 3.2 시나리오 그래프 (작가) — 철도 배선

챕터 간선이 작가의 판에 **오렌지 철도**로 미러된다. 규칙 하나: **한 구간에 선 하나.**

```
[EP01] ●선택지 ──▶[씬A]──(작가 배선)──▶[씬B]────●──▶[EP02]
        레일: 입구까지     속: 배선 하나      합류선: 레인 끝으로
```

- **칩**(`● 라루를 믿는다` / `○ 진행`)이 배선 진입점. 누르면 상세 + 자유 씬 배선 플라이아웃
- 가지 순서 = **읽는 순서**(대본 OPTION 순서 → 유령 → 진행 → 엔딩 스텁)
- 레일은 **첫 씬 입구까지만** 댄다. 웹 속은 작가의 실행 배선이 유일한 선
- 웹의 끝(기본 출구 없음 = 진행)에서 **합류선**이 레인 끝으로 모인다
- 스텁: `⏹ 종료`(길이 여기서 끝남) · `▢ {Id} (동기화 전)`(간선은 있는데 노드가 아직 없음)
- 도착 포트 — 여러 가지가 도착 카드 앞 접점 하나로 수렴. 누르면 들어오는 길 목록

### 3.3 노드 시각 체계 (2026-08-15)

색 하나가 아니라 **등뼈(왼쪽 색 막대) + 아이콘 + 카드 형태** 세 채널을 겹친다.
정의는 `GraphEditorView.CardStyleFor` 한 곳. 미니맵·접힌 목록도 같은 언어를 쓴다.

| 종 | 등뼈 | 형태 | 아이콘 |
|---|---|---|---|
| 엑셀노드 | 호박 | **미색 종이, 각진 모서리(4)** | 📄 |
| 자유 씬 | 청록 | 흰 종이, 둥근 모서리(10) | ✎ |
| 설정(조건) | 파랑 | 옅은 파랑 | ⚙ |
| 연출 | 보라 | 옅은 보라 | 🎬 |
| 연출 공급 | 초록 | 옅은 초록 | 🧰 |

---

## 4. 코드 지도 (이 층에서 자주 여는 곳)

| 하려는 일 | 파일 |
|---|---|
| 시나리오 그래프 철도·카드·미니맵 | `src/Vn.App/Views/GraphEditorView.axaml.cs` (`DrawChapterRails`·`DrawRailsFrom`·`DrawRailBranch`·`DrawAdvanceReturns`·`CardStyleFor`) |
| 챕터 그래프 화면·포트·편집 패널 | `src/Vn.App/Views/ChapterGraphView.axaml{,.cs}` (`DrawOptionPorts`·`PortY`·`ApplyEdgeFromPanel`·`RenameSelectedEpisode`) |
| 그래프에 무엇이 그려지는가 | `src/Vn.Authoring/Graph/GraphProjectionBuilder.cs` — 화면이 아니라 **여기가 정한다** |
| 엑셀 → 노드 동기화 | `src/Vn.Authoring/Chapters/EpisodeSyncService.cs` (`Sync`·`SupplyChapterConditionsToBoard`·`ConditionSupplyNodeName`) |
| 챕터 워크북 읽기·쓰기 | `Chapters/ChapterWorkbookReader.cs` · `ChapterWorkbookWriter.cs` |
| 구판 워크북 자동 이행 | `Chapters/ChapterWorkbookMigrator.cs` — 구→v5→v6→v7→v8 한 번에, .bak. 앱 Reload가 챕터 폴더 전체에 돌린다 |
| 대본 파일 자리·입양 | `Chapters/EpisodeLibrary.cs` (`FolderFor(project, chapterId)` = `episodes/{챕터}/` · `AdoptFlatWorkbook`) |
| 조건식 분해·조립·연산자 | `Chapters/ConditionExpressionParser.cs` (`TryDecomposeSingle` — 시트 세 칸 ↔ 식 문자열) |
| 검증·도달성 증명 | `Chapters/ChapterValidator.cs` · `ChapterReachabilityProver.cs` |
| 에피소드 워크북 생성·개명 | `Chapters/EpisodeLibrary.cs` |
| 모델을 바꾸는 유일한 통로 | `src/Vn.Authoring/Editing/ProjectEditor.cs` |
| 출구 포트 계산 | `src/Vn.Authoring/Flow/NodeConnections.cs` |

전체 구조와 "이 기능은 어디" 표는 [`ARCHITECTURE.md`](../../ARCHITECTURE.md).

---

## 5. 작업 관습 (반드시 지킬 것)

1. **모든 동작 변경에 run-log 항목 + 고정 테스트.** run-log는 커밋 메시지보다 상세한
   결정 기록이다. 소유자가 "왜 이렇게 됐나"를 여기서 따라간다.
2. **앱이 실행 중이면 빌드가 잠긴다.** `Vn.App.exe` 잠금 오류가 나면 **절대 강제 종료하지
   말고** 소유자에게 앱을 닫아 달라고 요청한다.
3. **PowerShell 5.1** — 커밋 메시지에 큰따옴표를 쓰지 말고 단일 인용 here-string(`@'...'@`)을
   쓴다. `&&`·삼항 연산자 없다.
4. 커밋 메시지 끝에 `Co-Authored-By: Claude <noreply@anthropic.com>`.
5. **실데이터로 재현한다.** 소유자 보고("~가 안 보인다")는 `DevSync\test3`를 임시 폴더로
   복사해 헤드리스로 그려 보면 대부분 즉시 재현된다(스크래치 테스트 후 삭제).
6. 소유자의 판단이 내 분석과 어긋나면 **소유자 판단을 참으로** 두되, 모순·오류는 지적한다
   (2026-08-14 소유자 지시).

---

## 6. 소유자의 실데이터

`C:\Users\river\Documents\DevSync\test3` — 실제로 쓰고 있는 프로젝트.

- 챕터 `3_ch00`: 에피소드 7개(시작에피소드 · EP01 · Ep02_A · Ep02_B · 엔딩 · new01 · rrr)
- 시작에피소드의 선택지 둘(`라루를 믿는다` → new01, `손을 잡는다` → rrr)
- **알려진 정리거리**:
  - `new01.xlsx`·`rrr.xlsx`는 대사 한 줄짜리라 판에 노드가 안 선다
    ("대사 0줄 워크북은 노드를 만들지 않는다" 규칙 — 대사를 쓰고 저장하면 선다)
  - `new02.xlsx`는 개명 사고의 잔재(원고 "t/w"). 살릴 것 없으면 삭제
  - EP01은 챕터에서 지워졌는데 판에 노드가 남아 있다 — 레일은 안 그려진다(정상)

---

## 7. 진행 상태

### 닫힌 것

- **Gate A·B·C 전부** — 리더/모델, 대본→노드 동기화, 검증기+도달성 증명, 내보내기
- **챕터 v2/v3/v4** — 엑셀 소유권 확정, 왼쪽 패널 = 챕터 목록, 툴은 워크북에 안 쓴다
- **2단계 무대(작가 계층)** — 엑셀노드 잠금, 포트 개통, 조건 자동 공급, 챕터 박스, 접기
- **시나리오 타임라인 T1·T2** — 철도 배선 + 포트 이사. **T3(자동 배치)은 철회**
  (소유자: "굉장히 어색하다"). 코드는 `5841eac`에 보존, 되돌린 커밋 `49be938`

### 열려 있는 것

| 항목 | 상태 |
|---|---|
| **⚠ 미커밋 작업 (2026-08-16)** | v5 규격 + 편집 UI 개편 + 선택지 v6이 **워킹 트리에 미커밋**으로 있다(화자 시트는 `5f77b30`으로 커밋됨). 견본 `chapter-graph-sample.xlsx`도 이행본. 이어받는 세션은 커밋부터 정리할 것 |
| **⚠ 런타임 계약 개정 (Gate D 짝)** | v5·v6이 계약을 세 군데 바꿨다: ① 내보내기 Op에 `GreaterThan`/`LessThan` 신규 ② bool 스탯(0/1, Equal) ③ 전이 규칙 = 고른 선택지 → 보이지 않는 기본(방어장치) → 종료. 수입기(유니티 저장소, 아직 없음)와 함께 확정해야 한다 |
| **시나리오 그래프의 v6 잔향** | 철도 칩·가지 순서가 아직 간선의 파생 `OptionLabel`로 돈다(동작함). 칩 순서를 선택지 시트 인덱스로 옮기는 정리는 미착수. 대본 CHOICE/OPTION 기반 유령 표시도 폐지 수순과 함께 정리 대상 |
| **Gate D (런타임 관통)** | 유니티 저장소의 수입기 부재. 스키마 1:1은 테스트로 닫음 |
| **G9 런타임 잔여** | ① 선택지 전이 구현(v6 규칙) ② `StatChanges` 원자 커밋 ③ Tier2→Yarn 단방향 브리지 |
| **⚠ Tier 2 영속화** | `EpisodeSelectionStateData`가 어디에도 저장되지 않는다 — 소유자 판단 대기 |
| **T3 재시도 여부** | 소유자가 "어느 부분이 어색했는지" 아직 답하지 않음. 코드는 `5841eac`에 보존 |
| **조건 삭제 기능** | 소유자가 **보류** 결정 (기록만) |
| 합류선 밀집 | 웹이 커지면 레인 끝에 합류선이 몰릴 수 있다. 계단식 분산 또는 웹 접기를 검토 |

### 다음에 소유자가 할 일 (데이터 쪽 — test3)

1. 앱을 켜면 `3_ch00`·`3_ch01`이 v6으로 자동 이행된다(.bak) — 이행 후 한 번 훑어볼 것
2. `3_ch00`의 남은 오류: 조건 `프롤로그봄`의 식이 비어 있음(이행이 안 고침 — 사람 몫)
3. `new01`·`rrr`에 대사를 써서 저장 → 시나리오 판에 노드가 섬 · `new02.xlsx` 정리 · 판에 남은 EP01 노드 처리

---

## 8. 마지막 커밋 이후 (2026-08-16, 미커밋 — run-log 상세)

한 날에 규격이 세 단계로 굴렀다. **순서가 곧 근거다**:

1. **화자 시트** (`5f77b30` 커밋됨) — 챕터 `화자` 시트 → 에피소드 워크북 화자 드롭다운
2. **v5 규격** — 간선 열 재배치·인덱스 폐지·조건 구조화(스탯/연산자/값)·스탯 타입·`<` `>`·픽스처 제거·자동 이행기
3. **편집 UI 개편** — "엑셀에서만 편집" 체크(기본 켬, 툴 읽기 전용) · 챕터 클릭 = 엑셀 열기 · 단추 없는 저장 · 탭 유지 · [＋] 연결 폼
4. **선택지 v6 → v7** — 선택지 정본이 챕터 `선택지` 시트로. v6은 칸의 주인을 간선으로 잡았으나 **v7에서 정정**: 칸 수는 에피소드(`선택지수`)가 정하고, 간선은 칸과 1:1(D열 = 칸 인덱스), 선택지 시트에 도착 없음

그 이전 커밋 흐름(2026-08-15): `79466df` 인수인계 · `8836124` 한 구간에 선 하나 ·
`f82beaf` A계층 격리 · `6f8940b` 낙하 폐지(→ v6에서 방어장치로 개정) 등 — run-log 참조.
