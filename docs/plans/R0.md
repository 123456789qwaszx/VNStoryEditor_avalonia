# R0. 소비자 오라클 최신화 — 계획서

> 상위 계획: [PLAN.md](PLAN.md). 상태: **완료 (2026-09-02)**. 작업 저장소: `VNStoryEditor_avalonia`만.

## 1. 목표와 한 줄 요약

에디터 안의 `Ked.Progression`을 현재 Unity 런타임 사본과 맞춰, **에디터가 허용한 JSON을 게임이 거부하는 틈을 먼저 닫는다.**

끝나면: 현재 런타임 DTO·로더·불변식이 에디터의 마지막 export 관문으로 실행되고, 제거된 레거시 타입에 에디터 코드가 기대지 않는다.

## 2. 선행 조건

- [x] 런타임 기준 브랜치 `server_DB`와 기준 커밋 확인.
- [x] 에디터 사본이 SceneId·VerifySceneEntries·Fold 이전이라는 실측.
- [x] 착수 직전에 런타임 최신 SHA `d53cc2f58423ba6e83fabfabbe88f1f547272380`을 다시 기록.
- [x] 다른 저장소는 수정하지 않고 위 기준 커밋을 반입 정본으로 고정.

## 3. 숙련자의 사고흐름

1. **기능 추가보다 심판 교체가 먼저다.** 낡은 심판 위에 Scene을 구현하면 잘못된 콘텐츠가 초록으로 보인다.
2. **파일 몇 개를 골라 복사하지 않는다.** 타입들은 함께 변한다. 반입 단위는 `Ked.Progression` 전체다.
3. **Unity 호스트 코드는 가져오지 않는다.** 순수 진행 코어만 에디터 테스트에서 소비한다.
4. **컴파일 오류를 목록으로 사용한다.** 제거된 Ending/Scenario API와 레거시 DTO 의존부를 숨기지 않고 드러낸다.
5. **규칙을 에디터에 재작성하지 않는다.** `CoreRefusals`가 실제 로더 진단을 엑셀 위치로 번역한다.

## 4. 작업 목록과 순서

| # | 작업 | 주요 위치 | 상태 |
|---|---|---|---|
| R0-0 | 양쪽 사본 파일·공개 API diff와 기준 SHA 기록 | 작업 기록 | 완료 |
| R0-1 | 기존 vendoring 규약과 csproj 경계 확인 | `src/Ked.Progression` | 완료 |
| R0-2 | 런타임 사본 통째 재반입 | `src/Ked.Progression` | 완료 |
| R0-3 | 제거 타입 의존 정리 | Exporter·tests | 완료 |
| R0-4 | DTO 1:1 대조 | Chapter/Stat/Node/Option/Condition/Change | 완료 |
| R0-5 | CoreRefusals 경로→워크북 행 번역 회귀 | Exporter tests | 완료 |
| R0-6 | 퇴화 Scene 기존 샘플 검증 | golden fixtures | 완료 |
| R0-7 | 전체 테스트·커밋 | solution | 완료 |

## 5. 특이사항

- 런타임 사본이 현재 실질 정본이다. 별도 `ked-progression` 저장소는 기준으로 쓰지 않는다.
- 에디터 사본에 남은 `EndingRule`, `ScenarioAdvance`, `ScenarioTransition`은 새 계약이 아니다.
- `Ked.Presentation.Core` 동기화와 섞지 않는다. 이번 단계는 진행 코어만 다룬다.
- Unity 전용 타입이나 비동기 호스트 계층이 들어오면 경계를 잘못 잡은 것이다.

## 6. 주의사항

| 증상 | 원인 | 대응 |
|---|---|---|
| 일부 파일만 바꿔 컴파일은 됨 | 구 API가 우연히 남음 | 전체 파일 목록과 공개 타입 대조 |
| 에디터 테스트만 초록 | 런타임 신규 불변식 픽스처 없음 | Scene·Auto 거부 픽스처 추가 |
| 진단 위치가 사라짐 | 코어 Path 형식 변경 | LocateInWorkbook 매핑 테스트 |
| 역사 문서까지 대량 수정 | 구현과 기록 정리 혼합 | 계약 코드만, 문서는 R5 |

## 7. 완료 기준

- [x] 반입 원천 SHA와 파일 목록이 `src/Ked.Progression/UPSTREAM.md`에 기록됨.
- [x] 에디터·런타임 `Ked.Progression`의 의미 있는 소스 차이가 설명됨.
- [x] 솔루션 Release 빌드 및 전체 테스트 1,799개 통과.
- [x] SceneId 없는 구판 JSON이 에피소드별 퇴화 Scene으로 로드됨.
- [x] 잘못된 Scene/Auto 샘플을 코어가 거부하고 간선 시트·행 진단으로 변환함.
- [x] 제거된 레거시 진행 API의 컴파일 참조가 제품 코드에 없음.

## 8. 테스트 기록

검증 커밋: `9863d379c5f46cbae4e48205e59056e39a95f060`  
[GitHub Actions 실행 #33631024592](https://github.com/123456789qwaszx/VNStoryEditor_avalonia/actions/runs/33631024592)

| 순서 | 검증 | 결과 |
|---|---|---|
| 1 | `dotnet restore VnTool.sln` | 통과 |
| 2 | `dotnet build VnTool.sln --no-restore --configuration Release` | 통과, 경고 0·오류 0 |
| 3 | Ked.Presentation.Core.Tests | 60/60 통과 |
| 4 | Vn.Core.Tests | 356/356 통과 |
| 5 | Vn.Authoring.Tests | 977/977 통과 |
| 6 | Vn.App.Tests | 406/406 통과 |
| 7 | 전체 | **1,799/1,799 통과** |
| 8 | 런타임 원천과 에디터 사본 `*.cs` 대조 | 제외 목록 외 차이 없음 |
| 9 | 구판 Scene fallback·Scene/Auto 거부 위치 회귀 | 통과 |

## 9. 하지 않는 것

Scene 워크북/UI, Auto 워크북/UI, 서버 API, 런타임 코드 수정.

## 10. 다음 단계

R1이 최신 소비자 오라클 위에서 SceneId를 저작·출력한다.
